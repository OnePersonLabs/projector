You are the Projector candidate implementer for checkpoint 1 in an isolated fresh repository. Read `PARTICIPANT.md` and its founding brief. Do not read any existing Psychord implementation, the baseline trial, an oracle, or files outside this repository except the fixed released Projector plugin at `C:\dev\projects\projector\.temp\projector-release-check\plugin`.

The plugin is the fixed candidate under evaluation. Read its `AGENTS.md`, `$projector` skill, and `$projector-change` skill. Invoke it only as `node C:\dev\projects\projector\.temp\projector-release-check\plugin\scripts\projector.mjs`. Start by retrieving context for the founding outcome. Because this new repository has no accepted meaning, use the authorized founding brief to propose, preview, and apply the initial readable canonical meaning with `$projector-change`; retain explicit unknowns rather than inventing claims. Use ordinary Codex edits to implement it. Retrieve/retain context before material edits and run Projector `check` after relevant behavior checks. Do not use OpenSpec.

Build a fresh minimal TypeScript browser application. Keep dependencies small, implement the public facade/contract and a real Web Audio gesture path, and write deterministic headless tests. The recognition-evidence count must never become a learning/mastery claim. Before source edits create `REHEARSAL_CONTEXT.md`; after work update `REHEARSAL_CHECKS.md` with exact commands/statuses, Projector context/check results, and honest browser limits. Commit the completed checkpoint; if the execution sandbox blocks Git metadata, report the exact error without bypassing it. Final response: changed files, checks, Projector IDs/results, failures/unknowns, commit hash.

This foundation run has a 30-minute wall-time ceiling and targets at most 40,000 generated tokens. Make inspection and command output concise, avoid broad browser exploration, and report generated and uncached input accounting when the host exposes it.
