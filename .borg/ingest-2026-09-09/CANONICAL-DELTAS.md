# Proposed canonical meaning and architecture deltas

Revised 2026-09-10. These are reviewable semantic changes, not accepted records. The [wrap-up plan](../../.temp/projector-wrap-up-plan-2026-09-09.md) controls execution. Typed canonical records own meaning; typed contracts generate exact schemas. Historical `PROJECTOR_SPEC` remains transitional evidence and acceptance-inventory input until its final cutover.

Preserve existing IDs, provenance, conditions, exceptions and future commitments. Reconcile retained context and inspect current hashes before selecting an edit. Revise an actual contradiction explicitly; do not append incompatible new wording beneath a superseded mandatory sandbox or interface statement. Generate active envelopes through existing canonical tooling, never with fabricated approval/evidence/hash fields.

## Existing Projector meaning to refine

The IDs below are established recipient identities. Retrieve their current records and dependencies during the owning change; this design does not freeze a stale file hash.

| Owner / existing ID | Proposed semantic refinement | Completion boundary |
|---|---|---|
| P1 `requirement:runtime-evidence-legibility` | Exercise a concrete accepted application scenario against an identifiable source/build. Record actual setup, endpoint, controller, DOM/storage/trace, tool/configuration identity, currentness, outcome and owned-resource cleanup. For the first adapter, state trusted workspace/host execution and its absence of malicious same-user or OS/network confinement protection. Keep unavailable observation distinct from failed behavior. | Actual collector and consumers implemented and verified; align scope with real modules and preserve unimplemented acoustic/device/latency/learning obligations. |
| P1/P2 `requirement:evidence-bound-completion` | A worker note, process exit, screenshot or historical receipt does not establish current behavioral fulfillment. Derive relevant continuation and missing-evidence questions from accepted meaning, current observations and existing lifecycle state. | Existing completion/representation consumers show the real scenario and fresh-session evidence without a new pass ledger. |
| P1/P2 `requirement:scoped-invalidation` | Apply currentness to actual source/build, scenario, controller, fixture/configuration and tool dependencies needed by a reused claim. Relevant dirty edits under unchanged HEAD and changed query membership matter. Bound broadly when the observable read cone is broad; preserve uncertainty. | Real positive/negative reuse cases through existing state-binding/reconciliation services. |
| P3 `requirement:scoped-reconsideration` | Use observed counterexamples to investigate the implicated code, oracle, tool, context or architecture. A host causal hypothesis remains a hypothesis until a discriminating check supports it. Reusable enforcement needs concrete recurrence, an active producer or a specific safety invariant, with intentional variants and counterevidence considered. | One actual host investigate/propose/fix/verify/reconcile loop; architectural promotion only if that case justifies it. |
| P3 `requirement:reverse-reconciliation` | Preserve failure evidence and use existing observation categories to guide scoped repair. A failed setup, stale claim, behavioral assertion failure and incomplete transaction require different actions. Repair suggestions alone do not close an accepted obligation. | Existing impact/coverage/knowledge and lifecycle paths carry the actual result; no classifier or incident queue. |
| P4 `requirement:self-hosted-value` | Exercise later-change/currentness survival and record available actual time, retries, context and repair effort. Report missing monetary components as unknown. Broader controlled comparisons may support a future advantage claim but are optional research, not a useful-release prerequisite. | Relevant existing tests/reports and a real spec-absent Projector change followed by an affected change in a fresh session before retirement; no mandatory trajectory driver or paid-model experiment. |

No new `state-bound-contributions` requirement is proposed. Host preparation and coordinator review use existing context, representation and exact-change authority. Notes are advisory. The attachment initiative is rejected, with no deferred implementation or revival mechanism. Existing proposal/validator identity and authenticated recovery remain owned by the current lifecycle, `requirement:scoped-invalidation` and `requirement:reverse-reconciliation`; Tasks 4, 7A and 8.4 exercise them through the retained runner.

## Settled host-execution revision: Tasks 2.2 and 4.7

This is a selected implementation direction, not an instruction to investigate whether a new sandbox is needed. Task 2.2 accepts the following precise semantic revision through the existing canonical lifecycle; Task 4 implements it and closes its code/spec/evidence consequences. Preserve existing entity IDs and authenticated history. Revise current titles and statements to describe host-executed validation. Record the later user direction and its trust tradeoff as the reason for replacement; do not rewrite the old investigation as a failed experiment or manufacture fresh approval/evidence metadata.

| Existing record | Exact intended change |
|---|---|
| `decision:windows-wsl-bubblewrap` | Replace the active mandatory WSL/bubblewrap choice with: "Execute Projector operations and validators through the host's configured permissions, using the native Node runner on Windows and the Linux Node runner for direct WSL. Projector does not require WSL bridging or provide a confinement backend. Preserve exact reviewed mutation authority, actual source/state/version checks, bounded time/output, caller cancellation and journaled recovery. Report only guarantees that the selected execution path establishes." Update the selected option, consequences and governing authority reference consistently. |
| `authority:windows-wsl-bubblewrap` | Replace current WSL-availability/immutable-overlay assumptions and the preserve conclusion with the accepted host-execution rationale. Retain the prior evaluated alternatives, bounded probe results and reasons in authenticated history/provenance. Current rationale records that trusted workspace/host code can interfere with collection, and that pinning/hashing does not prevent malicious same-user tampering. Do not carry a standing native-sandbox research obligation into the new decision. |
| `concept:controlled-validation-isolation` | Replace the mandatory denied-network/undeclared-path/read-only-overlay/WSL contract and prohibition of host execution with the host-execution contract above. Keep validator provenance/currentness and bounded execution explicit. Describe what is checked separately from what the host enforces; an ordinary process is not a security boundary. |
| `concern:cross-platform-validation-sandbox` | Replace its question with "How does controlled validation preserve change integrity and honest evidence under the configured host permissions on native Windows and direct WSL?" Update the selected option and resolution to the host route. Include actual execution, knowledge-validation, coverage, change compilation/execution and packaged-runner paths in its and the decision's scope, using the resulting source layout. |
| `scenario:recover-sandbox-rationale` | Replace the expected WSL answer with retrieval of the current host-execution choice, its retained integrity guarantees and limits, and why the prior WSL route was superseded. The forbidden result is silently restoring mandatory confinement or claiming that host execution enforces it. Exercise fresh context for a validator/approval change as well as a launcher change, so the rationale is actually discoverable. |
| `requirement:runtime-evidence-legibility` | Replace "isolated application instance" with "an identifiable application test instance with owned setup/teardown, an allocated endpoint and fresh test state under explicit host trust assumptions". Retain the required observations, source/build/scenario identity, collection method, freshness and failed-run diagnostics. Remove any implied mandatory adversarial confinement from this application's contract. |
| `scenario:inspect-isolated-application-behavior` | Revise title and setup to identifiable, separately owned test resources under host permissions. Keep actual observations mandatory. Add wrong-build/endpoint, contaminated prior state, unavailable collection and interrupted cleanup as failures; retain meaningful application outcome checks. This does not discharge acoustic/device/learning obligations. |

Keep `relation:concept:controlled-validation-isolation:governed-by:decision:windows-wsl-bubblewrap` valid under these retained IDs. Update actual related scope/relations and derived views; no sandbox lens currently exists, and `lens:core-static-dependencies` remains applicable. The filenames are existing content-addressed record paths, resolved by these IDs through the supported lifecycle; do not hand-edit envelope hashes or invent a parallel authority store.

Task 4.7 cannot close until the retained public paths consume this choice. Current concrete consumers include `packages/runtime/src/execution/command-executor.ts`, both sandbox launchers, `packages/control-plane/src/change-lifecycle/compiler.ts` and `executor.ts`, `knowledge/validators.ts`, `coverage/service.ts`, their contracts/tests, and plugin build/installation inputs. Remove mandatory confinement/overlay predicates from the host route while retaining checks of the exact validator source that actually runs. Audit capability, validation and certificate consumers for false isolation claims. Context/reconcile and coverage must work without a hidden sandbox prerequisite; metadata readiness must execute no validators.

Completion evidence is installed native Windows and direct WSL execution without the mandatory bridge, real failed/denied/cancelled paths, rejection of changed approved inputs or validators, and authenticated interruption recovery. Fresh context must retrieve the selected host rationale and its limits. Synchronize the affected transitional spec sections after implementation and review, then close Task 4.7. No unresolved confinement requirement for this selected route may be relabeled as future work. Genuine audio/device and other separately scoped future meaning stays discoverable without becoming a new wrap-up gate.

## First application scenario: derive from Psychord Omega's accepted concepts

Source: `C:/dev/projects/psychord-omega`, inspected HEAD `e3ed1c8ff30f4fb2b997b187dd4fe03d6af91c5f`. Accepted concept payloads live under `.projector/model/concepts`; the inspected model has concepts and relations, not requirements/scenarios directories. Do not describe the pilot as an already accepted scenario.

Derive and review one narrow scenario using the existing canonical lifecycle before implementing its browser checks. Its proposed meaning is:

- **Precondition:** the local browser application starts with fresh state, and the player uses the actual keyboard input and explicit save controls.
- **Trigger:** press C4 through `KeyA`, release, choose Keep this moment, reload, open Saved moments, enable sound with the real gesture and choose Listen.
- **Expected:** the selected player performance is stored locally and survives reload; saved timing/source attribution is retained; replay is visibly output and does not create new player performance history; save failure is reported without false success.
- **Forbidden:** an empty run fabricates a moment, replay becomes player evidence, storage failure becomes saved success, or observations of another build become evidence for this one.

Preserve links to `concept:personal-archive`, `concept:replay-provenance`, `concept:player-authorship`, `concept:local-privacy` and `concept:first-musical-minute`. The richer archive, learning vision and other unrealized input/sound capabilities remain distinct commitments. The scenario does not define a full Psychord remake or substitute browser trace for acoustic proof. [Exact pilot](APPLICATION-PILOT.md).

This is a separate preparatory canonical-only change with its own review/verification. Projector's existing `scenario:inspect-isolated-application-behavior` should be revised in its owning P1 batch to express identifiable test setup and actual host assumptions, with wrong-build, unavailable, failed-save and interrupted-cleanup cases. Isolation here means test-resource separation; do not preserve obsolete process-confinement claims through the title alone.

## Scenario reuse before new identities

Use current context to find existing meaning for fresh-session continuation, relevant dirty-tree invalidation, committed-publication recovery and alternate conforming implementations. Extend or exercise those records before creating duplicates. Later-change and shared-contract examples may remain tests of an existing obligation rather than new scenarios solely to mirror an implementation.

A new scenario is justified only by a distinct accepted outcome that an existing identity cannot express. Do not add donor-branded graph, anti-slop, domain-agent or geometric-reasoning entities.

## Architecture decisions and consequences

| Material decision | Selected route | Required evidence / reconsideration |
|---|---|---|
| Host execution and delivery | One bundled JS runner over existing TS services; host permissions; retire obsolete interfaces/mandatory bridge | Actual installed native Windows/direct WSL workflows, failed/denied/cancelled execution and preserved journals; revisit a missing host capability only when a real consumer requires it |
| Application collection | Owned snapshot + loopback static Node server + separate pinned Playwright controller, fresh browser state, external artifacts | Correct loaded bytes, DOM/storage/trace, wrong-build and cleanup controls; same-user trust remains explicit |
| Preparation and continuation | Host-native bounded work, one coordinator exact proposal, existing contexts/representations/recovery | Shared-contract review and fresh-session currentness; existing exact lifecycle, with advisory preparation and no contribution importer or scheduler |
| Scoped correction | Host uses existing categorized observations, then proposes/fixes/verifies/reconciles | A reproduced discrepancy and real correction; new architectural enforcement is conditional |
| Later-change validation | Relevant survival cases and lightweight actual cost reporting | Subsequent change preserves old behavior and distinguishes stale from violated; optional broader research has separate scope |

Accept only decisions with actual scope, alternatives, reasons and linked consequences. Reuse current concern/decision/authority shapes and supported relation kinds. A design table is not a grant of execution authority.

## Applying these deltas

1. Reconcile the current context and resolve the selected existing meaning and actual consumers.
2. Prepare the precise canonical/spec/status changes with the owning implementation; accept narrow preparatory meaning separately when it must precede code, such as the Omega scenario.
3. Implement and verify the bounded owning change through the supported lifecycle. At its completion, apply corresponding canonical refinements, affected historical sections and accurate capability descriptions in the same batch. Do not leave competing completed descriptions.
4. Rebase exact patch files when earlier wrap-up tasks touch their targets. Old patch applicability and review receipts do not certify the rebased result.
5. Before retirement, complete full useful-meaning parity and migrate live inventory/check consumers in coherent units. Execute the two real reserved self-development increments through the installed runner in the source-absent 10.8 candidate, then apply P2/P4 at 7A.6/10.9. Test the final exact staged cutover tree and revalidate any affected proof before deleting `PROJECTOR_SPEC`. Commit the reviewed/tested tree, confirm an actual-final-commit clone and deliver the bounded Psychord remake handoff.

No product or application canonical records are changed by this planning document.
