# Integrated implementation order

Revised after final ordering review, 2026-09-10. The [wrap-up plan](../../.temp/projector-wrap-up-plan-2026-09-09.md) owns task status. This map clarifies dependencies; numeric task order is not a requirement to implement consumers before their prerequisites.

| Stage | Exact owners | Result and spec synchronization |
|---|---|---|
| Resolve current intent and reserve real self-development work | 1--2, design boundaries in 5--6 | Bounded drift/consumer audit, initial canonical/spec correction, shared runner/readiness/evidence contracts. Task 2.1 reserves the real 7A.2 continuation increment and 7A.5 subsequent currentness increment for implementation at 10.8. If already present, select a genuinely needed pair from actual drift here. |
| Implement safe shared execution and upgrades | 3--6 after shared contracts | Task 2.2 accepts the seven named host-execution canonical revisions; 4.7 closes their real runtime/validator/coverage/compiler and spec consequences. Installed runner and host execution preserve source/state/approval/journal controls. Readiness precedes new loaders/writers. Migration exclusion must account for actual old binaries, including a tested quiescence/restart route. Seal every actual persisted-format change before promoting its writer, including later evidence/representation changes. Remove obsolete interfaces and mandatory bridge as consumers move. |
| Connect the actual application | 7 / P1 | Accept the narrow Omega scenario, implement host-owned build/server/controller and evidence consumers, exercise real behavior and negative controls. Apply the refreshed P1 patch at 7.7 after those tasks pass. No Docker/confinement project. |
| Prepare durable views and finish source parity | 8.4, remaining 8, then 9 | Ordinary reading and preapproval plan-instruction inspection share current representation ownership. Preserve complete valuable meaning and future commitments; replace actual spec consumers and fabricated authority checks. These services and source-absent baseline must work before implementing the reserved continuation surface. |
| Exercise the actual correction loop | 7B / P3, when an earlier real failure supplies evidence | Investigate, fix and verify through existing host/authority routes. Generalization is conditional. Apply P3 at 7B.6 after the real loop passes. This can occur before all other stages finish. |
| Implement the reserved two-change sequence with the spec absent | 10.8 executes 7A.2 and 7A.5; other 7A integration uses 4.6/8.4 | First implement bounded saved-context/lifecycle continuation through the installed runner in the source-absent candidate. In a fresh session, implement affected currentness explanations and preservation of independently current work. Exercise real old/new behavior, dirty/query/profile drift and recovery. Bring required changes back through normal current capture/review, without transferring clone approvals. |
| Close continuation and acceptance-method batches | 7A.6 immediately after 10.8, then 10.9 / P2 and P4 | Apply the refreshed P2 patch when continuation's required representation and survival cases pass. Then apply P4's method/limits patch after the real rehearsal and reporting pass. No required delta remains scheduled after retirement. |
| Review and retire the transitional source | 11, then 12.1--12.4 | Reconcile final clause-level parity and all applied deltas. Ingest meaning/migrate consumers in earlier coherent commits. Test the exact staged cutover tree in a clean candidate, including deletions/new files, not a normal HEAD clone. Revalidate rehearsal dependencies, review independently, commit only the tested tree, then run actual-final-commit identity/installed smoke checks. |
| Hand off the remake and retire campaign; optional scratch cleanup | 12.5, then 13 | A fresh installed session retrieves Omega's full accepted intent, data-preservation needs and concrete next vertical slice. Full musical-product rebuilding is separate. After preserving needed meaning/evidence and proving operation without scratch directories, 13.1 removes the assimilation campaign and empty `.borg` root. Optional `.temp` cleanup at 13.2 retains existing exact-list confirmation and does not block product readiness. |

Preparatory canonical acceptance precedes implementation where it defines the intended behavior or authority. Each implemented and verified change ends with its affected transitional spec delta in the same completion batch. Earlier changes invalidate patch bases: compare intent/current code/canonical meaning, refresh exact source hashes and hunks, review and check before applying. Do not force old patches, bulk-apply the combined artifact or maintain another spec after retirement.

Use $projector-lean-delivery with root-owned architecture/integration, bounded independent work and a separate read-only reviewer. Do not run a full suite at every handoff or create a standing hierarchy. Rebuild before `dist` consumers and finish coherent units with applicable tests, build/verify, installed behavior and reconciliation. Commit those units rather than saving all changes for cutover.

Functional self-development requires the real two-change sequence before retirement; final candidate checks repeat affected proof when dependencies change. Economic superiority remains unproved optional research. Missing platform behavior, measurements or artifacts remain unavailable, not zero cost or fabricated success.
