# Historical-spec changeset verification

Endpoint: design only. Commands ran in `C:/dev/projects/projector` on 2026-09-10. Actual `PROJECTOR_SPEC`, product code and canonical records remain unchanged.

1. `python -X utf8 .borg/ingest-2026-09-09/spec-deltas/build_patches.py`: exit 0. Source hash preconditions passed. Produced 21 file deltas with exclusive ownership: P1=9, P2=5, P3=4, P4=3. One new module; no deleted module or directory.
2. `git apply --check` on each of `P1.patch`, `P2.patch`, `P3.patch`, `P4.patch` and `PROJECTOR_SPEC.patch`: all exit 0. [Machine receipt](spec-apply-checks.json). No patch applied to the repository.
3. `git apply --stat .../PROJECTOR_SPEC.patch`: 21 files, 208 insertions, 10 deletions. Deltas include actual replacements of historical authority claims and focused additions to owning modules.
4. Python `check_spec.py` against the copied review tree: exit 0, 46 manifest/index files, 149 exported declarations, zero blocking diagnostics, 135 review warnings. [Output](spec-check.txt). This validates manifest inclusion, local links, module sizes, declaration uniqueness and mechanical prose constraints. An initial run found sentence/semicolon/style violations in new prose; the source transformations were corrected and the final regenerated tree passed.
5. `checkAuthoritativeSpecification(reviewTreeRoot)` from the actual repository script, imported under Node 24.20.0: exit 0, 46 files, zero blocking findings, 33 advisory findings. [Machine receipt](spec-engine-check.json). The Python and engine lint counts describe different checks; neither is a product-implementation certificate.

The package was generated by the coordinator after W5's delegated followup remained pending initialization and was interrupted before it wrote any artifacts. Exclusive ownership was transferred before generation. This execution detail does not change the design or receipt claims.

No generator, runtime schema, package, active model, acceptance workflow or application behavior was implemented. Real implementation must update the applicable executable contracts, schemas, consumers and checks through the tasks in the proposals. Spec applicability and style checks cannot prove collector isolation, oracle correctness, compatibility or economic value.
