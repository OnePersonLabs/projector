---
context: phase
phase: 01-slice-0-foundation-and-correctness-substrate
task: 0
total_tasks: 13
status: paused_at_plan_revision_gate
last_updated: 2026-08-07T10:42:52.634Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered by independent plan review.
> Acknowledge and structurally resolve each one before executing Phase 1.

- [ ] CONSTRAINT: Required engine tier — `KERN-052` requires `packages/engine -> packages/core`; the current drafts make engine optional and put selector/StateBinding evaluation in core — create the engine package, move runtime semantic evaluation there, compose it from CLI, and enforce the exact dependency graph.
- [ ] CONSTRAINT: Nyquist contract missing — validation is enabled but `01-VALIDATION.md` does not exist — create it and align every plan's verification commands/evidence with it.
- [ ] CONSTRAINT: Interrupted research revision — the revised deterministic package gate is present, but one stale `post-checkpoint for fast-check` phrase remains — remove the contradiction and verify the completed research artifact before replanning.

**Do not begin product-code execution until all boxes are checked and the independent plan checker passes.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Optional required tier | The plan treated `packages/engine` as optional despite the locked reference graph. | blocking | Make source-snapshot contract checks part of plan validation; enforce `engine -> core` in package-graph tests. |
| Validation by implication | Detailed test prose existed, but the required machine-readable Nyquist artifact was absent. | blocking | Generate `01-VALIDATION.md` before plan approval and cross-check every plan against it. |
| Interrupted artifact inconsistency | A revision changed the package-approval policy but left one stale checkpoint reference. | advisory | Run terminology/contradiction grep and a fresh independent plan check after any interrupted revision. |

<current_state>

The autonomous run is paused in Phase 1 after research, pattern mapping, a 13-plan draft, and one independent plan-checker pass. No plan has been approved for execution and no product code has been written. All 116 Phase 1 requirement IDs are assigned exactly once across the draft plans. The checker returned 4 blockers and 4 file-budget warnings. A research revision was started and interrupted at the user's request; it successfully added deterministic package legitimacy rules and resolved the SQLite/root-capability questions, but did not create `01-VALIDATION.md`.

</current_state>

<completed_work>

- Full authoritative ingestion: 44 docs, 376 headings, 1,319 unique v1 requirements, all mapped once to 14 committed phases.
- Phase 1 context committed as `4b9643d`.
- Phase 1 research committed as `52638b0` and normalized as `9a6dc26`; current research has additional uncommitted revisions.
- `01-PATTERNS.md` maps 27 implementation units.
- Thirteen draft plans, `SKELETON.md`, and `01-SOURCE-AUDIT.md` were generated.
- Mechanical plan checks passed for 116/116 ownership, acyclic dependencies, waves, structure, tracer-first shape, no-exec, rebuild, crash recovery, scoped binding, and property-test coverage.
- Independent checker identified the blockers preserved below.

</completed_work>

<remaining_work>

1. Finish `01-RESEARCH.md`: remove the stale `post-checkpoint` wording and verify package-legitimacy, `node:sqlite`, and `RootCapabilities` decisions are internally consistent.
2. Create `01-VALIDATION.md` with Nyquist commands, evidence, frequencies, and coverage for all Phase 1 plans.
3. Revise the 13 plans so `packages/engine` is mandatory, owns selector evaluation and StateBinding/query validation, CLI composes it, and package-graph tests enforce `engine -> core`.
4. Re-run the independent plan checker. Address the four file-budget warnings if practical without harming vertical slices.
5. Commit plans only after the checker returns `## VERIFICATION PASSED`.
6. Execute/verify Phase 1, then continue Phases 2–14.

</remaining_work>

<decisions_made>

- The entire 14-phase program is committed scope. Roadmap order is dependency sequencing, never permission to omit or hand-wave work.
- Projector is complete only after Phase 14 imports the GSD semantics into canonical `.projector/`, proves semantic equivalence/coverage, governs its own repo, and makes `PROJECTOR_SPEC/`, `.planning/`, and project-local GSD safely removable.
- Codex and Claude Code integrations are mandatory before handoff.
- `PROJECTOR_SPEC.md` and `INDEX.md` are non-authoritative and were intentionally excluded.
- Use TypeScript/Node 24 ESM, pnpm, Zod plus JSON Schema, SQLite derived state, canonical JSON plus SHA-256, Vitest, and fast-check.
- Dependency installation is autonomous only after deterministic exact-version provenance/integrity/license/lifecycle-script checks; mismatches fail closed.
- `node:sqlite` stays behind `DerivedStore`; a failure triggers adapter replacement through architecture-decision machinery, not contract leakage or a database service.
- Filesystem case sensitivity is an observed per-root `RootCapabilities` fact, never inferred from the host OS.

</decisions_made>

<blockers>

- Required engine tier missing from draft architecture.
- `01-VALIDATION.md` missing.
- Research revision was interrupted and has one known stale phrase.
- Four non-blocking plan-size warnings: 01-01 (14 files), 01-05 (11), 01-09 (11), 01-13 (10).

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact pause state.
2. This file — blockers, remaining work, and resume order.
3. `01-CONTEXT.md` — locked Phase 1 boundary and no-deferral rule.
4. `01-RESEARCH.md` — implementation strategy; note the interrupted revision.
5. `01-PATTERNS.md` and `01-SOURCE-AUDIT.md` — source-grounded file analogs and requirement ownership.
6. `01-01-PLAN.md` through `01-13-PLAN.md` — unapproved draft execution plans.
7. `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/SPEC-COVERAGE.md` — authoritative GSD projection and traceability.
8. `.planning/intel/source-snapshot/SHA256SUMS` and relevant snapshot source docs — exact bootstrap source evidence.

## Critical Anti-Patterns (do NOT repeat these)

- Do not reinterpret later phases as optional deferral; all 1,319 v1 requirements are committed.
- Do not execute draft plans until the engine-tier and Nyquist blockers are resolved and independently rechecked.
- Do not treat global digest changes as blanket staleness; scoped value/query/program dependencies govern binding validity.
- Do not let open/sampled/unavailable discovery prove absence or completion.
- Do not allow Projector-generated conformity, history alone, or inferred repetition to create authority.

## Infrastructure State

- Branch: `gsd-attempt` (created during the GSD attempt; it points at the original `michael-jennings/issue1` tip plus the handoff commits).
- No product scaffold, installed dependencies, database, server, watcher, or external job exists.
- No subagent remains running; the research agent was interrupted deliberately.
- The only untracked research cache is `.planning/research/.cache/4238dcde6844789f0b9eb88d3f3ac43fd9e998c59f17bc6bf06b95e019366a81.json`.
- Working tree contains the uncommitted Phase 1 plans/patterns/audit/skeleton plus ROADMAP and research revisions; preserve them.

## Pre-Execution Critique Required

- Design artifacts: `01-01-PLAN.md` through `01-13-PLAN.md`.
- Critique focus: mandatory engine ownership/package graph, Nyquist artifact alignment, research consistency, and plan file-budget warnings.
- Gate: Do not begin execution until a fresh independent plan checker returns `## VERIFICATION PASSED`.

<context>

The project-planning ingestion is complete and high-fidelity. The autonomous implementation attempt was intentionally stopped early because the workflow overhead was too slow for the user. The safest transfer point is the Phase 1 plan-review gate: substantial planning work is preserved, but no implementation side effects have occurred.

</context>

<next_action>

Start with: finish the three blocking constraints above, then run a fresh independent Phase 1 plan review before executing any code.

</next_action>
