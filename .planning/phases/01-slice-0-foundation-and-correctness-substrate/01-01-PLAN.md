---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - vitest.workspace.ts
  - packages/core/package.json
  - packages/core/src/ports/foundation-store.ts
  - packages/runtime/package.json
  - packages/runtime/src/foundation/foundation-store.ts
  - packages/cli/package.json
  - packages/cli/src/main.ts
  - packages/testkit/package.json
  - packages/testkit/src/fixture-repository.ts
  - packages/testkit/test/walking-skeleton.test.ts
autonomous: true
requirements: [CLI-001, CLI-002, DELV-001, DELV-002, DELV-003, DELV-004, DELV-005, DELV-006, DELV-007, SLICE-000, SLICE-008, SLICE-009, METR-002, METR-003]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maintainer can run one CLI process that writes canonical config, rebuilds SQLite from it, and returns deterministic terminal or JSON status."
    - "The first behavior test fails before implementation and passes after the complete path exists."
    - "Every run reports canonical config and toolchain digests without executing repository code."
  artifacts:
    - path: "packages/cli/src/main.ts"
      provides: "Host-neutral CLI composition root"
      exports: ["runCli"]
    - path: "packages/runtime/src/foundation/foundation-store.ts"
      provides: "Canonical config write plus SQLite rebuild/read tracer"
      exports: ["createFoundationStore"]
    - path: "packages/testkit/test/walking-skeleton.test.ts"
      provides: "Black-box init/status acceptance path"
  key_links:
    - from: "packages/cli/src/main.ts"
      to: "packages/core/src/ports/foundation-store.ts"
      via: "Injected FoundationStore port"
      pattern: "FoundationStore"
    - from: "packages/runtime/src/foundation/foundation-store.ts"
      to: ".projector/config.json and .projector/state.db"
      via: "canonical write followed by derived rebuild"
      pattern: "rebuild"
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Prove the Projector walking skeleton through a real local CLI invocation.

Purpose: Establish the whole-application tracer required by CLI-001, CLI-002, SLICE-000, SLICE-008, and SLICE-009 before horizontal expansion.
Output: An executable pnpm/TypeScript workspace whose `init` and `status` path performs one canonical write, one SQLite rebuild/read, and deterministic terminal/JSON reporting.
</objective>

<execution_context>
@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md
@/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-CONTEXT.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-RESEARCH.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/SKELETON.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/10-operation/cli-modes-and-security.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive init → canonical write → SQLite rebuild → status through one CLI process</name>
  <reversibility rating="costly">The initial CLI/port boundary becomes the seam consumed by every later Phase 1 plan, but remains internal and versionable.</reversibility>
  <files>packages/core/src/ports/foundation-store.ts, packages/runtime/src/foundation/foundation-store.ts, packages/cli/src/main.ts, packages/testkit/test/walking-skeleton.test.ts</files>
  <read_first>
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/10-operation/cli-modes-and-security.md
  </read_first>
  <behavior>
    - RED: spawning `projector init --repo <fixture> --format json` fails because no CLI/store exists.
    - GREEN: init creates validated `.projector/config.json`, builds `.projector/state.db` from that canonical input, and returns exit 0 JSON containing command, policy, configDigest, toolchainDigest, canonicalDigest, and graphRevision.
    - `projector status --format text|json` is read-only and returns the same canonical digest and indexed record count in deterministic key/line order.
    - Deleting `state.db` then invoking status with rebuild enabled reconstructs it from canonical input; repository package scripts are never executed.
  </behavior>
  <action>Write the black-box Node 24 test first and confirm it fails for the missing executable path per DELV-001; this dependency-free RED gate lets the tracer prove built-in filesystem/crypto/SQLite behavior before workspace installation. Define the narrow `FoundationStore` port in core and implement it in runtime with `node:fs`, `node:crypto`, and `node:sqlite`; the CLI must inject the concrete store, parse only `init`/`status` plus `--repo` and `--format text|json`, map failures to the documented exit categories, and emit no nondeterministic timestamps. Keep repository content as inert data and use no shell interpolation. The later Vitest workspace must retain this black-box case as an acceptance command rather than replace its behavior.</action>
  <verify>
    <automated>node --test packages/testkit/test/walking-skeleton.test.ts</automated>
  </verify>
  <acceptance_criteria>The test log demonstrates RED before GREEN; init and status both succeed through the real CLI entry point; terminal and JSON values agree; deleting SQLite does not alter canonical meaning.</acceptance_criteria>
  <done>The end-to-end walking skeleton is committed with a runnable CLI, canonical write, SQLite rebuild/read, and observable output.</done>
</task>

<task type="auto">
  <name>Task 2: Verify dependencies and establish the strict ESM root workspace</name>
  <reversibility rating="costly">Workspace and module-resolution choices affect every package, but the package graph is still repository-internal.</reversibility>
  <files>package.json, pnpm-lock.yaml, pnpm-workspace.yaml, tsconfig.base.json, vitest.workspace.ts</files>
  <read_first>
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md
    @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-PATTERNS.md
  </read_first>
  <action>Before installing, query npm registry metadata, repository URLs, dist integrity, and lifecycle scripts for `zod@4.4.3`, `typescript@7.0.2`, `vitest@4.1.10`, `fast-check@4.9.0`, and the resolved exact Node-24 `@types/node` release; record exact resolutions in `pnpm-lock.yaml`, refuse unexpected install scripts, and perform the explicitly authorized script-disabled install. Declare pnpm package discovery, Node 24 engines, strict ESM compilation, `noUncheckedIndexedAccess`, declaration output, and Vitest projects. Point every root script at repository-owned commands and keep build/test commands noninteractive.</action>
  <verify>
    <automated>pnpm exec tsc -p tsconfig.base.json --noEmit &amp;&amp; node --test packages/testkit/test/walking-skeleton.test.ts</automated>
  </verify>
  <acceptance_criteria>Strict type checking and the walking-skeleton test pass under Node 24 ESM with no circular workspace dependency.</acceptance_criteria>
  <done>The dependency set and root workspace are reproducible and ready for package manifests.</done>
</task>

<task type="auto">
  <name>Task 3: Add core, runtime, CLI, and fixture package shells around the proven path</name>
  <reversibility rating="reversible">Package manifests and fixture helpers can be changed behind the already proven composition boundary.</reversibility>
  <files>packages/core/package.json, packages/runtime/package.json, packages/cli/package.json, packages/testkit/package.json, packages/testkit/src/fixture-repository.ts</files>
  <read_first>
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md
    @.planning/intel/source-snapshot/PROJECTOR_SPEC/11-validation/testing-and-adversarial-evaluation.md
  </read_first>
  <action>Create the core, runtime, CLI, and testkit package boundaries required by the proven path; the analyzer package is created with its no-exec capability in 01-09. Expose a testkit helper that creates isolated Git fixture roots, invokes the CLI with explicit argv/cwd/env, captures stdout/stderr/exit code, and cleans up safely. Preserve DELV-004 by creating no host, provider, network, or broad semantic analyzer implementation.</action>
  <verify>
    <automated>pnpm -r exec tsc --noEmit &amp;&amp; node --test packages/testkit/test/walking-skeleton.test.ts</automated>
  </verify>
  <acceptance_criteria>All declared packages build and test; the helper runs the real CLI without shell command construction or shared fixture state.</acceptance_criteria>
  <done>The repository is left governable by the next contract and storage slices per DELV-006.</done>
</task>

</tasks>

<prohibitions>
- Do not add a web UI, daemon, hosted service, authentication surface, graph database, or deployment workflow.
- Do not execute repository scripts during init/status observation.
- Do not make SQLite or CLI types part of core domain contracts.
- Do not use repository content to alter policy or authorize writes.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| argv/repository → CLI | Untrusted paths, flags, and repository bytes enter policy and I/O. |
| canonical files → SQLite | Canonical authority is parsed into disposable derived state. |
| npm registry → workspace | Third-party package metadata and tarballs enter the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | `FoundationStore` paths | high | mitigate | Root parameter is explicit; only governed `.projector` targets are written; later path policy replaces the narrow tracer behind the port. |
| T-01-02 | Elevation of privilege | CLI process invocation | high | mitigate | Explicit argv/cwd/env, no shell interpolation, no repository-code execution. |
| T-01-03 | Repudiation | CLI reports | medium | mitigate | Deterministic config/toolchain/canonical digests and documented exit categories. |
| T-01-SC | Tampering | pnpm installs | high | mitigate | Registry/repository/integrity/lifecycle-script verification, exact lockfile, script-disabled install, and explicit user authorization. |
</threat_model>

<verification>
Run `pnpm -r exec tsc --noEmit`, `pnpm -r test`, and the documented local CLI command against a fresh testkit fixture.
</verification>

<success_criteria>
- A real CLI invocation completes the canonical write/rebuild/read loop.
- Terminal and JSON output are deterministic and equivalent.
- The workspace uses the fixed stack and the lockfile records exact verified artifacts.
- Every task begins from an observable failing or missing capability and ends with a runnable user capability.
</success_criteria>

<artifacts_produced>
- Root pnpm/TypeScript/Vitest workspace configuration
- Core foundation port, runtime adapter, CLI composition root
- Reusable isolated fixture harness and walking-skeleton acceptance test
</artifacts_produced>

<output>
Create `.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-01-SUMMARY.md` when done.
</output>
