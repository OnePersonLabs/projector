---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - packages/core/src/domain/base.ts
  - packages/core/src/domain/intent.ts
  - packages/core/src/schemas/base.ts
  - packages/core/src/schemas/intent.ts
  - packages/core/test/base-contracts.test.ts
  - packages/core/test/intent-contracts.test.ts
  - packages/core/test/spec-composition.test.ts
  - scripts/check-spec-composition.ts
autonomous: true
requirements: [CORE-002, CORE-003, CORE-006, KERN-001, KERN-002, KERN-003, KERN-006, KERN-007, KERN-008, KNOW-029]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every base, Concept, Relation, Requirement, and BehavioralScenario serialized field has one Zod-backed definition."
    - "The authoritative spec entry/manifest order and generated-bundle status are checked deterministically during bootstrap without becoming runtime authority."
    - "Intent, lens, surface, and observed-shadow concepts remain distinct and cross-module references import singular definitions."
    - "Requirement/Scenario identity survives representation wording changes."
  artifacts:
    - {path: "packages/core/src/schemas/base.ts", provides: "Exact base and causal-origin schemas"}
    - {path: "packages/core/src/schemas/intent.ts", provides: "Concept, Relation, Requirement, and Scenario schemas"}
    - {path: "scripts/check-spec-composition.ts", provides: "Deterministic source authority/order/bundle checker"}
  key_links:
    - {from: "packages/core/src/domain/intent.ts", to: "packages/core/src/schemas/intent.ts", via: "z.infer type derivation", pattern: "z\.infer"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Create the singular bounded contract modules for canonical intent and identity.

Purpose: Satisfy the composition, bounded-use, plane-separation, and early canonical entity contracts without duplicating definitions.
Output: Exact Zod schemas, inferred TypeScript types, and behavior-first contract tests.
</objective>

<execution_context>
@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md
@/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-RESEARCH.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/identity-and-relations.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Parse one canonical Requirement through the base contract graph</name>
  <reversibility rating="costly">These public serialized field names are consumed by all later slices and require coordinated schema migrations if changed.</reversibility>
  <files>packages/core/src/domain/base.ts, packages/core/src/schemas/base.ts, packages/core/test/base-contracts.test.ts, packages/core/test/spec-composition.test.ts, scripts/check-spec-composition.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-001–003); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/identity-and-relations.md</read_first>
  <behavior>
    - EntityId, Confidence, ContentHash, SourceClass, EvidenceRef, SemanticSignature, and every CausalOrigin literal parse exactly.
    - Confidence rejects values outside 0..1 and ContentHash rejects unversioned/non-SHA-256 strings.
    - The exported TypeScript types are inferred from the same Zod schemas used at runtime.
    - The checker treats `SPEC.md` plus manifest-ordered modules as authoritative, excludes `INDEX.md`, regenerates the convenience bundle deterministically, and rejects missing/duplicate/conflicting canonical contract ownership.
  </behavior>
  <action>Write failing table-driven schema tests and a failing source-composition fixture, then implement the exact base and causal-origin fields from KERN-002/KERN-003 plus a bootstrap-only composition checker for CORE-002. Export JSON-schema-ready Zod objects and `z.infer` types; use imported singular definitions rather than copied field unions. The checker must read the authoritative entry/manifest order, verify the generated bundle byte construction and navigation-only exclusion, and report cross-module duplicate/contradictory ownership without making the source corpus a product runtime dependency. Do not label Confidence as calibrated probability.</action>
  <verify><automated>pnpm vitest run packages/core/test/base-contracts.test.ts packages/core/test/spec-composition.test.ts</automated></verify>
  <acceptance_criteria>Every required literal accepts, representative misspellings/range violations reject, composition fixtures prove entry/manifest/bundle authority, and the schema module has no runtime adapter dependency.</acceptance_criteria>
  <done>The base contract graph can validate one Requirement's nested origin/evidence/hash references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Concept and Relation as independently owned semantic facts</name>
  <reversibility rating="costly">Concept and Relation wire shapes are public contracts but remain versioned and migration-capable.</reversibility>
  <files>packages/core/src/domain/intent.ts, packages/core/src/schemas/intent.ts, packages/core/test/intent-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (CORE-006, KERN-006, KERN-007); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/identity-and-relations.md</read_first>
  <behavior>
    - Concept accepts only the 17 specified kinds and four lifecycle states.
    - Relation accepts only the 23 specified relation types and never implies governance merely by existing.
    - Endpoint references remain stable IDs and carry source class, evidence, confidence, activity, and semantic hash.
  </behavior>
  <action>Extend the failing intent-contract tests, then implement Concept and Relation schemas/types with exact required fields and literals. Keep relation fact/hypothesis semantics separate from rule/applicability authority per CORE-006; store no path-derived identity in the contract.</action>
  <verify><automated>pnpm vitest run packages/core/test/intent-contracts.test.ts -t "Concept|Relation"</automated></verify>
  <acceptance_criteria>Valid Concept/Relation fixtures round-trip; missing fields and unsupported discriminators reject with deterministic issue paths.</acceptance_criteria>
  <done>Concepts and Relations have singular importable definitions with no cross-plane collapse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define Requirement and BehavioralScenario as canonical behavior identity</name>
  <reversibility rating="costly">Behavior identity contracts become durable canonical records used by all behavior projections.</reversibility>
  <files>packages/core/src/domain/intent.ts, packages/core/src/schemas/intent.ts, packages/core/test/intent-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-008, KNOW-029); @.planning/intel/source-snapshot/PROJECTOR_SPEC/03-knowledge/relevance-and-change-cognition.md</read_first>
  <behavior>
    - Requirement and Scenario accept only candidate/active/deprecated/rejected/superseded states.
    - IntentOriginRef and scenario-step role literals are exact.
    - A representation text change cannot create a second canonical Requirement or Scenario type/owner.
  </behavior>
  <action>Add RED cases for Requirement, BehavioralScenario, IntentOriginRef, and scenario steps, then implement the exact fields. Keep canonical behavior in these records and expose no Markdown/Gherkin document as a competing authority. Document module ownership in exports so consumers import these definitions instead of re-declaring them.</action>
  <verify><automated>pnpm vitest run packages/core/test/intent-contracts.test.ts</automated></verify>
  <acceptance_criteria>Exact fixtures validate, malformed origin/step/status inputs reject, and only one module owns each public definition.</acceptance_criteria>
  <done>Canonical behavior identity is schema-valid, bounded, and representation-independent.</done>
</task>
</tasks>

<prohibitions>
- No handwritten JSON Schema or parallel DTO authority.
- No `any`, path-based identity, executable selector/query payload, or runtime adapter import.
- No broad product workflow implementation in this contract slice.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Serialized contract input → core schemas | Untrusted JSON attempts to become canonical intent/identity data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | Public schema parsing | high | mitigate | Strict Zod discriminators, range/hash validation, deterministic issue tests. |
| T-02-02 | Spoofing | Stable identity fields | medium | mitigate | Stable IDs are explicit fields; names, paths, and aliases cannot substitute. |
| T-02-03 | Elevation of privilege | SourceClass/Relation | medium | mitigate | Tests prove observed/inferred facts and Relations do not gain authored/governance authority. |
| T-02-SC | Tampering | Dependencies | high | mitigate | No new package install; use the verified lock from 01-01. |
</threat_model>

<verification>`pnpm vitest run packages/core/test/base-contracts.test.ts packages/core/test/spec-composition.test.ts packages/core/test/intent-contracts.test.ts &amp;&amp; pnpm exec tsc -p tsconfig.base.json --noEmit`</verification>
<success_criteria>All exact base and intent contracts validate from bounded modules with one owner per definition and generated-schema compatibility.</success_criteria>
<artifacts_produced>Bootstrap composition checker; base/causal contracts; Concept/Relation/Requirement/Scenario schemas and inferred types; unit fixtures.</artifacts_produced>
<output>Create `01-02-SUMMARY.md` when done.</output>
