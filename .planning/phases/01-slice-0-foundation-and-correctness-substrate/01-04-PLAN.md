---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - packages/core/src/domain/representation.ts
  - packages/core/src/domain/surface.ts
  - packages/core/src/schemas/representation.ts
  - packages/core/src/schemas/surface.ts
  - packages/core/test/representation-contracts.test.ts
  - packages/core/test/surface-contracts.test.ts
autonomous: true
requirements: [KERN-032, KERN-039, RUNTIME-001, SURF-001]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Representation fingerprints expose assurance per protected dimension and can never overstate the weakest required dimension."
    - "Surface/enumeration contracts exist without importing or implementing external providers."
  artifacts:
    - {path: "packages/core/src/schemas/representation.ts", provides: "Profiles, projections, and preservation fingerprints"}
    - {path: "packages/core/src/schemas/surface.ts", provides: "Observability, enumeration, surface, artifact contracts"}
  key_links:
    - {from: "packages/core/src/schemas/representation.ts", to: "packages/core/src/schemas/base.ts", via: "ContentHash and evidence references", pattern: "ContentHash"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Define the exact early representation and surface contracts.

Purpose: Preserve truthful proof dimensions and unavailable-lane boundaries before renderer or provider implementations exist.
Output: Representation and surface schema modules with exact discriminators and invariant tests.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/03-knowledge/relevance-and-change-cognition.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/05-projections/runtime-and-representations.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/modernization-and-surfaces.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Enforce protected-dimension representation contracts</name>
  <reversibility rating="costly">Profiles/projections are public serialized contracts whose changes require coordinated consumers and migrations.</reversibility>
  <files>packages/core/src/domain/representation.ts, packages/core/src/schemas/representation.ts, packages/core/test/representation-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-032); @.planning/intel/source-snapshot/PROJECTOR_SPEC/05-projections/runtime-and-representations.md</read_first>
  <behavior>
    - Targets, 11 protected dimensions, 14 style-rule kinds, statuses, optimizations, and projection statuses are exact.
    - Fingerprint assurance is no stronger than the weakest required dimension and unsupported dimensions remain explicit.
    - Token accounting is optional and never changes source semantic identity.
  </behavior>
  <action>Start with failing schema and weakest-assurance invariant tests, then implement SemanticPreservationFingerprint, Profile, Projection, ProjectionRef, and token-accounting contracts. Implement the assurance check as a pure primitive independent of authority and context compression per RUNTIME-001.</action>
  <verify><automated>pnpm vitest run packages/core/test/representation-contracts.test.ts</automated></verify>
  <acceptance_criteria>Exact fixtures validate and any projection claiming stronger overall assurance than a protected dimension rejects.</acceptance_criteria>
  <done>Representation contracts preserve proof dimensions without implementing rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define surface and observability contracts without provider code</name>
  <reversibility rating="costly">Surface capability fields are public adapter boundaries, but concrete adapters remain replaceable.</reversibility>
  <files>packages/core/src/domain/surface.ts, packages/core/src/schemas/surface.ts, packages/core/test/surface-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-039, SURF-001); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/modernization-and-surfaces.md</read_first>
  <behavior>
    - ObservabilityClass accepts closed/bounded/open/sampled/unavailable only.
    - EnumerationContract, SurfaceCapabilities, Surface, and Artifact validate every exact field/literal.
    - observedAt remains informational and excluded from local semantic rebuild hashes by contract metadata.
  </behavior>
  <action>Write closed/bounded/open/unavailable fixtures, then implement the exact surface schemas/types and explicit semantic-projection metadata. Create no provider, network, host, cloud, registry, or app-store adapter implementation; SURF-001 requires the contract boundary now, not external behavior.</action>
  <verify><automated>pnpm vitest run packages/core/test/surface-contracts.test.ts</automated></verify>
  <acceptance_criteria>Surface/artifact fixtures parse, invalid access/kind/observability values reject, and the core package has no external provider dependency.</acceptance_criteria>
  <done>External/local surfaces can be described truthfully without performing external observation.</done>
</task>
</tasks>

<prohibitions>
- No inference engine, representation renderer, model call, provider adapter, network access, or external write.
- No assurance strengthening by aggregation or token reduction.
- No permissive untyped replacement for `StateBinding`.
</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Identity/relevance/representation/surface JSON → core | External or inferred records attempt to claim stronger identity, relevance, or proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | Representation assurance | high | mitigate | Weakest-dimension invariant and unsupported-dimension preservation tests. |
| T-04-02 | Information disclosure | Surface metadata | medium | mitigate | Schema separates informational timestamps/metadata from semantic projections; no external adapter runs. |
| T-04-03 | Spoofing | Relevance provenance | medium | mitigate | Required reason provenance, confidence, evidence IDs, unknowns, unavailable lanes, and bound state. |
| T-04-SC | Tampering | Dependencies | high | mitigate | No new install; verified lockfile only. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/representation-contracts.test.ts packages/core/test/surface-contracts.test.ts &amp;&amp; pnpm exec tsc -p tsconfig.base.json --noEmit`</verification>
<success_criteria>All assigned contracts validate exactly, expose truthful uncertainty/assurance, and introduce no provider implementation.</success_criteria>
<artifacts_produced>Representation and surface schema/type modules plus invariant tests.</artifacts_produced>
<output>Create `01-04-SUMMARY.md` when done.</output>
