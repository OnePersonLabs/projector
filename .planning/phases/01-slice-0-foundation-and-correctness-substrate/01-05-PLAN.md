---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04, 01-06]
files_modified:
  - packages/core/src/domain/state-binding.ts
  - packages/core/src/domain/relevance.ts
  - packages/core/src/schemas/state-binding.ts
  - packages/core/src/schemas/relevance.ts
  - packages/core/src/ports/index.ts
  - packages/core/src/registry/public-contracts.ts
  - packages/core/test/state-binding-contracts.test.ts
  - packages/core/test/relevance-contracts.test.ts
  - packages/core/test/ports-contracts.test.ts
  - packages/core/test/public-contract-registry.test.ts
  - scripts/export-json-schemas.ts
autonomous: true
requirements: [KERN-042, KERN-049, KERN-050, KERN-054, KNOW-010, KNOW-013, KNOW-014, SLICE-001, ACC-000]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every Phase 1 public serialized contract resolves through one registry entry and generated JSON Schema."
    - "StateDigest, value/query dependencies, result fingerprints, and StateBinding are exact and fully serializable."
    - "Identity resolution and relevance closures bind the exact StateBinding schema and preserve reasons, unknowns, and unavailable lanes."
    - "Core ports expose async graph/query/binding/transform/surface seams without concrete runtime dependencies."
  artifacts:
    - {path: "packages/core/src/schemas/state-binding.ts", provides: "Exact scoped-state contracts"}
    - {path: "packages/core/src/schemas/relevance.ts", provides: "Identity resolution and relevance closure contracts"}
    - {path: "packages/core/src/ports/index.ts", provides: "Core analyzer/graph/runtime/surface ports"}
    - {path: "packages/core/src/registry/public-contracts.ts", provides: "Machine-readable public contract registry"}
  key_links:
    - {from: "packages/core/src/registry/public-contracts.ts", to: "packages/core/src/schemas/*.ts", via: "explicit schema imports and references", pattern: "schema"}
    - {from: "scripts/export-json-schemas.ts", to: "packages/core/src/registry/public-contracts.ts", via: "z.toJSONSchema export", pattern: "toJSONSchema"}
    - {from: "packages/core/src/schemas/relevance.ts", to: "packages/core/src/schemas/state-binding.ts", via: "singular boundState schema", pattern: "StateBindingSchema"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Close the StateBinding/relevance public contract graph and define the ports every concrete adapter must implement.

Purpose: Make all early-slice serialized references machine-resolvable and keep runtime/analyzer/provider concerns outside core.
Output: Exact state-binding/relevance/supporting contracts, port interfaces, registry, generated JSON Schema exporter, and completeness tests.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md
@.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-02-SUMMARY.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-03-SUMMARY.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-04-SUMMARY.md
@.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-06-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Parse one complete StateBinding with value and query dependencies</name>
  <reversibility rating="costly">StateBinding is the shared validity contract for plans, approvals, capabilities, and projections.</reversibility>
  <files>packages/core/src/domain/state-binding.ts, packages/core/src/schemas/state-binding.ts, packages/core/test/state-binding-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-042); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md</read_first>
  <behavior>
    - StateDigest, nine value-dependency kinds, 13 query kinds, query/result/dependency records, StateBinding, and five validation statuses match the exact contract.
    - Query input is normalized serializable data and cannot contain functions/symbols/cycles.
    - Result fingerprints retain result count, observability, assumptions, unavailable lanes, and dependency keys even for empty results.
  </behavior>
  <action>Write failing nested fixtures and invalid-literal/nonserializable-input cases, then implement exact Zod schemas and inferred types. Resolve the lazy relevance `boundState` reference from 01-04 to this singular schema. Do not insert repository or canonical root hashes into local dependency arrays.</action>
  <verify><automated>pnpm vitest run packages/core/test/state-binding-contracts.test.ts</automated></verify>
  <acceptance_criteria>A complete binding round-trips through Zod/JSON; invalid query data and omitted proof-boundary fields reject deterministically.</acceptance_criteria>
  <done>The early contract graph contains a real dependency-scoped StateBinding schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate a bound identity resolution and relevance closure</name>
  <reversibility rating="costly">The identity/relevance wire shapes are consumed by closure, context, and planning engines.</reversibility>
  <files>packages/core/src/domain/relevance.ts, packages/core/src/schemas/relevance.ts, packages/core/test/relevance-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KNOW-010, KNOW-013, KNOW-014); @.planning/intel/source-snapshot/PROJECTOR_SPEC/03-knowledge/relevance-and-change-cognition.md; @packages/core/src/schemas/state-binding.ts</read_first>
  <behavior>
    - SemanticIdentityCandidate, NewSemanticBoundary, and SemanticIdentityResolution validate every exact field and outcome/kind literal.
    - RelevanceSeed, RelevanceReason, RelevanceEntry, and RelevanceClosure validate exact band/kind/provenance values and retain unknown/unavailable lanes.
    - `boundState` resolves the singular StateBinding schema and rejects an unvalidated object.
  </behavior>
  <action>Write failing nested fixtures for one resolution and closure after Task 1 establishes StateBindingSchema, then implement exact Zod schemas/types using base, intent, and state-binding imports. Keep reasons, provenance, assurance, unknowns, and unavailable lanes visible and deterministic; do not implement inference or relevance traversal in this contract task.</action>
  <verify><automated>pnpm vitest run packages/core/test/relevance-contracts.test.ts</automated></verify>
  <acceptance_criteria>All exact discriminators validate; missing confidence/evidence/unknown/binding fields reject; closure preserves all four relevance bands.</acceptance_criteria>
  <done>One identity resolution and relevance closure parse with a real dependency-scoped binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define core ports and prove the complete public contract registry</name>
  <reversibility rating="costly">Port signatures, registry keys, and generated schema identifiers affect multiple consumers but keep implementations replaceable.</reversibility>
  <files>packages/core/src/ports/index.ts, packages/core/test/ports-contracts.test.ts, packages/core/src/registry/public-contracts.ts, packages/core/test/public-contract-registry.test.ts, scripts/export-json-schemas.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-001, KERN-049, KERN-050, KERN-054, SLICE-001, ACC-000); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md</read_first>
  <behavior>
    - Analyzer/graph/query/binding/transform/surface ports and supporting contracts expose exact fields/methods; query/binding methods are asynchronous and execution remains state-bound.
    - SourceClass remains exactly authored/derived/observed/inferred and no port silently promotes it.
    - Every referenced Phase 1 public schema resolves exactly once or carries an explicit extension-defined marker.
    - Every registry entry exports valid JSON Schema from its Zod source and has a stable contract ID/version.
    - Recursive/cross-module references resolve without `any`, duplicate ownership, or dangling `$ref`.
  </behavior>
  <action>Write compile-time fake-port and registry-closure tests, then define exact core interfaces/support schemas and register every Phase 1 schema. Keep AbortSignal, state binding, allowed units, dry-run, operation evidence, approvals, validators, and external references; core imports no concrete adapter API. Implement sorted deterministic JSON Schema export and a deliberate broken-reference fixture proving CI failure.</action>
  <verify><automated>pnpm vitest run packages/core/test/ports-contracts.test.ts packages/core/test/public-contract-registry.test.ts &amp;&amp; node scripts/export-json-schemas.ts --check &amp;&amp; pnpm exec tsc -p tsconfig.base.json --noEmit</automated></verify>
  <acceptance_criteria>In-memory fakes implement all ports; registry has zero unresolved/duplicate references; generated schema is deterministic; synthetic missing references fail.</acceptance_criteria>
  <done>All core seams are fake-testable and ACC-000/SLICE-001 pass through the executable registry gate.</done>
</task>
</tasks>

<prohibitions>
- No `any`, executable query payload, global-root-only binding, duplicate schema owner, or concrete adapter import in core.
- No handwritten JSON Schema copies.
- No provider or host-specific port name.
</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Serialized state/query data → registry and core ports | Untrusted dependency/query records cross into validity and execution seams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Tampering | StateBinding input | high | mitigate | Strict serializability and discriminator tests; complete dependency digest inputs. |
| T-05-02 | Elevation of privilege | Core ports | high | mitigate | State-bound transform/surface plans and no concrete adapter dependency. |
| T-05-03 | Spoofing | Contract registry | medium | mitigate | Unique stable IDs/versions and closure/duplicate-reference checks. |
| T-05-SC | Tampering | Dependencies | high | mitigate | No new dependency; use verified Zod export API from lockfile. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/state-binding-contracts.test.ts packages/core/test/relevance-contracts.test.ts packages/core/test/ports-contracts.test.ts packages/core/test/public-contract-registry.test.ts &amp;&amp; pnpm exec tsc -p tsconfig.base.json --noEmit`</verification>
<success_criteria>All public references resolve, JSON Schema export is deterministic, and core ports are exact, async where required, and concrete-implementation-free.</success_criteria>
<artifacts_produced>StateBinding and relevance contracts; full port surface; public registry and schema exporter; registry/port tests.</artifacts_produced>
<output>Create `01-05-SUMMARY.md` when done.</output>
