---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - packages/core/src/domain/selector.ts
  - packages/core/src/schemas/selector.ts
  - packages/core/src/governance/evaluate-selector.ts
  - packages/core/test/selector-contracts.test.ts
  - packages/core/test/selector-evaluator.property.test.ts
autonomous: true
requirements: [GOV-001, GOV-002, GOV-003, GOV-004, GOV-005]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Selectors are canonical serialized data and contain no executable code."
    - "all/any/not/atom and every exact field/matcher literal evaluate deterministically."
    - "Semantic fields are first-class; path is only one allowed bootstrap dimension."
  artifacts:
    - {path: "packages/core/src/schemas/selector.ts", provides: "Recursive SelectorExpr schema"}
    - {path: "packages/core/src/governance/evaluate-selector.ts", provides: "Pure deterministic selector evaluator"}
  key_links:
    - {from: "packages/core/src/governance/evaluate-selector.ts", to: "packages/core/src/schemas/selector.ts", via: "parse before evaluation", pattern: "SelectorExpr"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Ship the canonical selector AST and deterministic evaluator required for governed applicability.

Purpose: Replace executable query payloads with inspectable, hashable, dependency-declarable data.
Output: Recursive Zod schema, inferred types, pure evaluator, and algebraic property tests.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/04-governance/scope-and-rules.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-02-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Parse and evaluate one nested semantic selector</name>
  <reversibility rating="costly">Selector wire syntax is persisted in rules/lenses and requires migrations if changed.</reversibility>
  <files>packages/core/src/domain/selector.ts, packages/core/src/schemas/selector.ts, packages/core/src/governance/evaluate-selector.ts, packages/core/test/selector-contracts.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (GOV-001–005); @.planning/intel/source-snapshot/PROJECTOR_SPEC/04-governance/scope-and-rules.md</read_first>
  <behavior>
    - Recursive all/any/not/atom forms parse with exact structure.
    - Atom field accepts exactly the 23 specified fields; matcher accepts exactly seven values; value remains unknown serialized data.
    - A nested semantic selector returns the same result for the same facts regardless of object insertion order.
    - Functions, symbols, cycles, and prototype-bearing executable payloads reject before evaluation.
  </behavior>
  <action>Write failing parse/evaluation cases, then implement a discriminated recursive Zod schema and a pure evaluator that receives a fact accessor/matcher registry through explicit arguments. Normalize regex/glob inputs deterministically and return match plus declared dependency keys/reasons; do not eval code or import filesystem/runtime functions.</action>
  <verify><automated>pnpm vitest run packages/core/test/selector-contracts.test.ts</automated></verify>
  <acceptance_criteria>All operations/fields/matchers work, invalid operations or executable payloads reject, and evaluation emits deterministic result/dependency reasons.</acceptance_criteria>
  <done>A real nested selector can be serialized, parsed, evaluated, and explained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove selector algebra and canonical evaluation properties</name>
  <reversibility rating="reversible">Property generators/tests can evolve without changing the stable selector contract.</reversibility>
  <files>packages/core/test/selector-evaluator.property.test.ts</files>
  <read_first>@packages/core/src/governance/evaluate-selector.ts; @.planning/intel/source-snapshot/PROJECTOR_SPEC/11-validation/testing-and-adversarial-evaluation.md</read_first>
  <behavior>
    - all/any results are permutation-independent for fixed items and facts.
    - double negation preserves result and dependency keys.
    - repeated evaluation of canonicalized input is byte-for-byte equal.
    - invalid regex and unavailable structural matcher return typed failure rather than nondeterministic throw/false proof.
  </behavior>
  <action>Add fast-check arbitraries for bounded selector trees/facts and assert algebraic/determinism properties with replayable seeds. Include explicit unavailable matcher capability and verify it widens the result rather than proving nonmembership.</action>
  <verify><automated>pnpm vitest run packages/core/test/selector-evaluator.property.test.ts</automated></verify>
  <acceptance_criteria>Properties pass across configured runs and failure output includes reproducible seed/path.</acceptance_criteria>
  <done>Selector evaluation is deterministic, inspectable, and truthful under unsupported capability.</done>
</task>
</tasks>
<prohibitions>- No `eval`, generated functions, closures in canonical selector data, filesystem access, or hidden global matcher registry. - No path-only scope model.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical selector data → evaluator | Untrusted match expressions attempt code execution, scope expansion, or denial of service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Elevation of privilege | Selector payload | high | mitigate | Serialized AST validation; executable values reject; pure evaluator. |
| T-06-02 | Tampering | Regex/glob matcher | medium | mitigate | Deterministic normalization and typed invalid/unavailable results. |
| T-06-03 | Denial of service | Recursive selectors | medium | mitigate | Bounded depth/item validation and AbortSignal/budget-ready evaluator API. |
| T-06-SC | Tampering | Dependencies | high | mitigate | fast-check already verified/pinned in 01-01. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/selector-contracts.test.ts packages/core/test/selector-evaluator.property.test.ts`</verification>
<success_criteria>Exact selector syntax and every required field/matcher are covered by examples and properties with no executable data.</success_criteria>
<artifacts_produced>Selector schema/types; evaluator; contract/property tests.</artifacts_produced>
<output>Create `01-06-SUMMARY.md` when done.</output>
