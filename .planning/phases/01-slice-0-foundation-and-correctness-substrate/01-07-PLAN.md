---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 07
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - packages/core/src/hashing/canonical-json.ts
  - packages/core/src/hashing/digests.ts
  - packages/core/src/identity/stable-identity.ts
  - packages/core/src/storage/canonical-manifest.ts
  - packages/core/test/canonical-json.property.test.ts
  - packages/core/test/digests.property.test.ts
  - packages/core/test/stable-identity.test.ts
  - packages/core/test/canonical-manifest.property.test.ts
autonomous: true
requirements: [KERN-004, KERN-005, KERN-015, KERN-017, SLICE-003, SLICE-004, TEST-002, TEST-003, TEST-037, TEST-038, TEST-044, ACC-003, ACC-004]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Canonical JSON bytes are independent of insertion and filesystem enumeration order."
    - "Document, semantic, discovery, query, dependency, and root hashes are versioned SHA-256 values over declared projections."
    - "Moves preserve identity; split/merge/replace/delete emit exact lineage and tombstone records."
  artifacts:
    - {path: "packages/core/src/hashing/canonical-json.ts", provides: "Versioned deterministic serializer"}
    - {path: "packages/core/src/hashing/digests.ts", provides: "Projection-aware SHA-256 functions"}
    - {path: "packages/core/src/storage/canonical-manifest.ts", provides: "Sorted root manifest/digest builder"}
  key_links:
    - {from: "packages/core/src/storage/canonical-manifest.ts", to: "packages/core/src/hashing/digests.ts", via: "hash sorted entity/document-hash entries", pattern: "canonicalDocumentHash"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Implement deterministic canonical serialization, distinct hash dimensions, stable identity, lineage, and root manifests.

Purpose: Provide the exact algebraic substrate for fine-grained state, rebuild comparison, and scoped invalidation.
Output: Pure hashing/identity/manifest modules with fast-check properties and acceptance cases.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/11-validation/testing-and-adversarial-evaluation.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-02-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Canonicalize and hash one entity independent of insertion order</name>
  <reversibility rating="costly">The versioned canonical byte contract determines every stored digest, but version prefixes permit controlled replacement.</reversibility>
  <files>packages/core/src/hashing/canonical-json.ts, packages/core/test/canonical-json.property.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-004, TEST-037); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - Semantically identical JSON values with arbitrary object insertion order serialize to identical UTF-8 bytes.
    - Arrays retain order; object keys use one documented Unicode ordering; unsupported/cyclic/nonfinite values reject.
    - No locale, platform path, timestamp, or filesystem enumeration affects output.
  </behavior>
  <action>Write RED example/property tests, then implement a versioned canonical JSON serializer for the schema-supported JSON domain. Document and test numeric/string/key semantics explicitly; use no locale-sensitive sorting and fail closed on unsupported values.</action>
  <verify><automated>pnpm vitest run packages/core/test/canonical-json.property.test.ts</automated></verify>
  <acceptance_criteria>Insertion-order properties pass with replayable seeds and malformed values fail deterministically.</acceptance_criteria>
  <done>One entity produces stable canonical bytes across repeated randomized construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute declared semantic, discovery, document, and dependency digests</name>
  <reversibility rating="costly">Hash profile/version changes invalidate stored identities and dependent proofs, but are explicitly versioned.</reversibility>
  <files>packages/core/src/hashing/digests.ts, packages/core/test/digests.property.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-004, KERN-015, TEST-002, TEST-044, ACC-003); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - SHA-256 output matches `sha256:v1:<64 lowercase hex>` and hashes canonical bytes only.
    - Schema-declared volatile timestamps/run IDs/local paths/UI metadata never change semantic hash.
    - Alias/display-only changes can change discovery/document hashes without changing semantic meaning hash.
    - Exact document and dependency/query hashes include their declared complete fields.
  </behavior>
  <action>Write failing projection-difference properties, then implement Node crypto SHA-256 helpers over explicit projection descriptors. Require each entity schema to declare semantic/discovery/document projections; reject missing projection metadata rather than hashing the complete object by convenience.</action>
  <verify><automated>pnpm vitest run packages/core/test/digests.property.test.ts</automated></verify>
  <acceptance_criteria>Volatile-field and alias-only metamorphic tests demonstrate the required dimension changes; exact content changes alter document identity.</acceptance_criteria>
  <done>Every hash role has a distinct named function, input projection, and versioned output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve identity/lineage and build order-independent root manifests</name>
  <reversibility rating="costly">Stable IDs and root-manifest format become persisted references, but versioned lineage/migration can evolve them.</reversibility>
  <files>packages/core/src/identity/stable-identity.ts, packages/core/src/storage/canonical-manifest.ts, packages/core/test/stable-identity.test.ts, packages/core/test/canonical-manifest.property.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-005, KERN-017, SLICE-003, SLICE-004, TEST-003, TEST-038, ACC-004); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - Authored IDs persist; derived IDs are adapter-namespaced; inferred IDs bind stable semantic key plus normalized evidence-set identity.
    - Move preserves ID; split/merge/replace/delete return exact LineageRecord/Tombstone shapes.
    - Root manifest sorts `(entityId, canonicalDocumentHash)` and is independent of file enumeration and physical sharding.
  </behavior>
  <action>Add RED example/property tests, then implement stable identity and exact lineage/tombstone constructors plus a derived manifest builder. The manifest is a returned derived value only and never editable authority. Include other canonical governance files as stable keyed entries without treating directory path as meaning.</action>
  <verify><automated>pnpm vitest run packages/core/test/stable-identity.test.ts packages/core/test/canonical-manifest.property.test.ts</automated></verify>
  <acceptance_criteria>Move/split/merge/replace/delete cases pass; shuffled and differently sharded entry sets produce identical root digests.</acceptance_criteria>
  <done>Identity survives physical change and exact snapshot identity is deterministic.</done>
</task>
</tasks>
<prohibitions>- No raw `JSON.stringify` hashing, custom crypto, locale sort, path-derived ID, editable root manifest, or whole-object semantic hash without schema projection.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated documents/projections → canonical bytes and digests | Field selection or ordering errors could spoof exact or semantic identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | Canonical serializer | high | mitigate | Property tests for order/platform independence and fail-closed unsupported values. |
| T-07-02 | Spoofing | Stable identity | high | mitigate | Explicit ID derivation namespaces/evidence-set binding and lineage/tombstones. |
| T-07-03 | Repudiation | Root digest | medium | mitigate | Sorted exact document-hash manifest distinct from semantic/discovery hashes. |
| T-07-SC | Tampering | Crypto/property dependencies | high | mitigate | Node crypto built-in and verified fast-check lock entry. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/canonical-json.property.test.ts packages/core/test/digests.property.test.ts packages/core/test/stable-identity.test.ts packages/core/test/canonical-manifest.property.test.ts`</verification>
<success_criteria>All assigned serialization/hash/identity properties pass and each digest dimension changes only for its declared inputs.</success_criteria>
<artifacts_produced>Canonical serializer; digest profiles; stable identity/lineage constructors; root manifest builder; properties.</artifacts_produced>
<output>Create `01-07-SUMMARY.md` when done.</output>
