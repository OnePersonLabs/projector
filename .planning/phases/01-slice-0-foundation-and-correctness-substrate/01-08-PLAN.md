---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 08
type: execute
wave: 6
depends_on: [01-01, 01-02, 01-07, 01-09]
files_modified:
  - packages/core/src/storage/canonical-store.ts
  - packages/core/src/storage/canonical-layout.ts
  - packages/core/src/schemas/canonical-document.ts
  - packages/runtime/src/storage/filesystem-canonical-file-port.ts
  - packages/core/test/canonical-layout.test.ts
  - packages/core/test/canonical-store.test.ts
  - packages/runtime/test/filesystem-canonical-file-port.test.ts
  - fixtures/canonical-locality/repository.json
autonomous: true
requirements: [PROD-012, PROD-018, PROD-030, KERN-012, KERN-013, KERN-014, KERN-016, KERN-018, KERN-020, SLICE-002, ACC-002]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Canonical authored/governance documents are independently addressable and SQLite/caches/observations remain derived."
    - "A bounded entity load/update reads and rewrites only that document plus derived manifest/journal inputs, never a project-wide model."
    - "Rebuild inputs are limited to repository/Git, committed canonical state, and an explicitly pinned external snapshot."
  artifacts:
    - {path: "packages/core/src/storage/canonical-layout.ts", provides: "Exact `.projector/` canonical/derived path classification"}
    - {path: "packages/core/src/storage/canonical-store.ts", provides: "Port-driven bounded canonical operations"}
    - {path: "packages/runtime/src/storage/filesystem-canonical-file-port.ts", provides: "Root-safe atomic file adapter"}
  key_links:
    - {from: "packages/core/src/storage/canonical-store.ts", to: "packages/runtime/src/storage/filesystem-canonical-file-port.ts", via: "CanonicalFilePort injection at CLI", pattern: "CanonicalFilePort"}
    - {from: "packages/runtime/src/storage/filesystem-canonical-file-port.ts", to: "packages/runtime/src/filesystem/root-path.ts", via: "all path resolution", pattern: "RootPath"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Implement the fine-grained canonical `.projector/` store and prove bounded locality.

Purpose: Make accepted intent durable, singularly owned, version-controlled, and sufficient to rebuild all derived state.
Output: Exact layout classification, canonical envelope/schema, port-driven store, root-safe file adapter, and locality fixture.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-07-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-09-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Write, load, and update one Requirement without loading the graph</name>
  <reversibility rating="costly">Canonical directory/envelope formats persist in Git and changes require deterministic previewable migration.</reversibility>
  <files>packages/core/src/storage/canonical-store.ts, packages/core/src/schemas/canonical-document.ts, packages/core/test/canonical-store.test.ts, fixtures/canonical-locality/repository.json</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-014, KERN-016, ACC-002); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - Put/get/update of one Requirement validates schema/version/stable ID/key/lifecycle/hash and touches only its file plus transaction/derived bookkeeping ports.
    - Filename/directory moves do not alter semantic identity.
    - A spying file port proves no full-directory/full-graph read or rewrite occurs.
  </behavior>
  <action>Create a failing locality fixture/test, then implement a core CanonicalStore over injected CanonicalFilePort, schema registry, hash profiles, and journal hook. Address by stable identity/kind; validate existing identity before update; calculate complete/semantic/discovery hashes explicitly; return a deterministic change preview and touched-path list.</action>
  <verify><automated>pnpm vitest run packages/core/test/canonical-store.test.ts -t "bounded|update|move"</automated></verify>
  <acceptance_criteria>The operation succeeds with a file port that throws on graph-wide enumeration and proves exactly one canonical entity write.</acceptance_criteria>
  <done>A bounded canonical entity can be created, loaded, and updated independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify every canonical and derived `.projector/` location</name>
  <reversibility rating="costly">The path contract is persisted repository structure, but deterministic migration can re-shard by stable ID.</reversibility>
  <files>packages/core/src/storage/canonical-layout.ts, packages/core/test/canonical-layout.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-012, KERN-013, KERN-018, SLICE-002); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - Exact canonical directories include config, four model kinds, rules/lenses/representations/authorities/concerns/decisions/preferences/exceptions/migrations/receipts.
    - plans/certificates/reports/generated/cache/state.db have exact ignored/opt-in/derived classifications.
    - Relations live independently from endpoints; stable-ID prefix sharding does not affect meaning.
    - A canonical kind descriptor declares at least one consequential semantic consumer category before the kind can be authored.
  </behavior>
  <action>Write a table-driven layout/consequence test, then implement canonical kind-to-path and path-to-classification functions using POSIX-relative paths only. Require each modeled kind descriptor to name at least one planning, applicability, invalidation, transformation, verification, explanation, architecture, authority, or completion consumer per PROD-018. Keep derived/ignored categories out of root manifest authority and ensure governance record payloads route through registered versioned schemas/envelopes rather than one monolithic document.</action>
  <verify><automated>pnpm vitest run packages/core/test/canonical-layout.test.ts</automated></verify>
  <acceptance_criteria>Every specified path has the correct authority/ignore classification and no project-wide semantic document path exists.</acceptance_criteria>
  <done>The complete canonical/derived boundary is executable and independently addressable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the root-safe filesystem canonical file port</name>
  <reversibility rating="reversible">Filesystem I/O details remain replaceable behind CanonicalFilePort and RootPath.</reversibility>
  <files>packages/runtime/src/storage/filesystem-canonical-file-port.ts, packages/runtime/test/filesystem-canonical-file-port.test.ts</files>
  <read_first>@packages/core/src/ports/index.ts; @packages/runtime/src/filesystem/root-path.ts; @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/canonical-state.md</read_first>
  <behavior>
    - Every read/write/list request resolves through RootPath and returns/records the checked real target.
    - Writes use same-root staging plus fsync/rename and reject out-of-root or symlink-escaped targets.
    - Rebuild input enumeration never reads live external state or hidden run history.
  </behavior>
  <action>Write failing adapter tests with an isolated fixture root, then implement the CanonicalFilePort using explicit UTF-8/canonical bytes and atomic same-root replacement. Allow only repository/Git/canonical plus explicit pinned-snapshot inputs per KERN-020; preserve deterministic error codes and touched paths.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/filesystem-canonical-file-port.test.ts</automated></verify>
  <acceptance_criteria>Safe reads/writes pass, escape/symlink/live-external cases refuse before mutation, and successful writes are durably replaceable.</acceptance_criteria>
  <done>CanonicalStore is wired to a root-safe adapter without leaking fs into core.</done>
</task>
</tasks>
<prohibitions>- No monolithic canonical model, SQLite authority, path-based semantic identity, live external rebuild read, or direct `node:fs` use in core. - No bounded operation may enumerate all semantic files.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical API caller → filesystem adapter | Validated semantic records cross into durable repository mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | Canonical files | high | mitigate | Schema/hash validation, change preview, atomic replace, journal hook. |
| T-08-02 | Elevation of privilege | File targets | high | mitigate | RootPath-only resolution and real-target/symlink checks before mutation. |
| T-08-03 | Repudiation | Bounded update | medium | mitigate | Deterministic touched-path/change preview and complete document digest. |
| T-08-SC | Tampering | Dependencies | high | mitigate | No new install; built-in fs and verified packages only. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/canonical-layout.test.ts packages/core/test/canonical-store.test.ts packages/runtime/test/filesystem-canonical-file-port.test.ts`</verification>
<success_criteria>Exact canonical locations exist, one entity updates locally, and every runtime write is root-constrained and deterministic.</success_criteria>
<artifacts_produced>Canonical layout/envelope/store; filesystem port; canonical-locality fixture and tests.</artifacts_produced>
<output>Create `01-08-SUMMARY.md` when done.</output>
