---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 09
type: execute
wave: 5
depends_on: [01-01, 01-04, 01-05]
files_modified:
  - packages/runtime/src/filesystem/root-path.ts
  - packages/runtime/src/process/argv-runner.ts
  - packages/runtime/src/git/git-reader.ts
  - packages/analyzers/package.json
  - packages/analyzers/src/filesystem/inventory.ts
  - packages/analyzers/src/git/observation.ts
  - packages/runtime/test/root-path.test.ts
  - packages/runtime/test/argv-runner.test.ts
  - packages/analyzers/test/no-exec-observation.test.ts
  - fixtures/path-safety/cases.json
  - fixtures/no-exec/package.json
autonomous: true
requirements: [SEC-001, SEC-002, SEC-006, SEC-007, SEC-008, SEC-009, SEC-010, SEC-011, SEC-012, OBSV-002, OBSV-003, SLICE-010]
estimate: {tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every filesystem target is POSIX-relative, normalized, checked against the real governed root, and records its real target."
    - "Drive, UNC, case-sensitivity, traversal, and symlink escape cases refuse before mutation."
    - "Filesystem/Git inventory treats repository content as data and never executes package/build/test/generated-code commands."
  artifacts:
    - {path: "packages/runtime/src/filesystem/root-path.ts", provides: "Single root/path safety utility"}
    - {path: "packages/runtime/src/process/argv-runner.ts", provides: "Explicit argv/cwd/scope/budget process seam"}
    - {path: "packages/analyzers/src/filesystem/inventory.ts", provides: "No-exec deterministic local inventory"}
  key_links:
    - {from: "packages/analyzers/src/git/observation.ts", to: "packages/runtime/src/process/argv-runner.ts", via: "injected explicit-argv runner", pattern: "ArgvRunner"}
    - {from: "packages/analyzers/src/filesystem/inventory.ts", to: "packages/runtime/src/filesystem/root-path.ts", via: "checked path port", pattern: "RootPath"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Establish the initialization-time trust boundary, root-safe path semantics, and no-exec observation wedge.

Purpose: Ensure untrusted repository and cross-platform path data cannot grant authority, escape scope, or execute code.
Output: RootPath, explicit argv runner, safe Git reader, filesystem/Git observation adapters, and adversarial fixtures.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md; @.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/10-operation/cli-modes-and-security.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-05-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Resolve safe and hostile POSIX/Windows/symlink paths against one governed root</name>
  <reversibility rating="costly">The path-safety result contract is shared by all later filesystem mutations, while implementation remains adapter-local.</reversibility>
  <files>packages/runtime/src/filesystem/root-path.ts, packages/runtime/test/root-path.test.ts, fixtures/path-safety/cases.json</files>
  <read_first>@.planning/REQUIREMENTS.md (SEC-006–012); @.planning/intel/source-snapshot/PROJECTOR_SPEC/10-operation/cli-modes-and-security.md</read_first>
  <behavior>
    - Accept valid POSIX-style relative paths and return normalized relative path, absolute checked path, real target, filesystem case-sensitivity observation, and symlink disposition.
    - Reject normalized traversal, absolute POSIX paths, Windows drive-relative/absolute forms, UNC/device paths, NULs, and write-through symlinks escaping root.
    - Probe actual filesystem case behavior and keep Windows syntax tests runnable under WSL without requiring native writes.
  </behavior>
  <action>Write the cross-platform fixture table and RED tests, then implement one RootPath service for read/write intent. Normalize syntax before joining, validate drive/UNC/device forms independent of host OS, compare real targets using observed case behavior, and enforce an explicit symlink policy before mutation. Return typed refusal codes and checked real target; never rely on a string-prefix check alone.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/root-path.test.ts</automated></verify>
  <acceptance_criteria>All safe cases resolve; every escape/drive/UNC/symlink/case fixture refuses or reports the specified observation before mutation.</acceptance_criteria>
  <done>One utility owns all repository-root path semantics and is usable on Linux/WSL/Windows paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run Git through explicit argv, cwd, scope, environment, and budget</name>
  <reversibility rating="reversible">Git/process implementation is replaceable behind an injected runner port.</reversibility>
  <files>packages/runtime/src/process/argv-runner.ts, packages/runtime/src/git/git-reader.ts, packages/runtime/test/argv-runner.test.ts</files>
  <read_first>@.planning/intel/source-snapshot/PROJECTOR_SPEC/10-operation/cli-modes-and-security.md (Command execution); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md</read_first>
  <behavior>
    - Runner accepts executable plus argv array, declared cwd/read scope/env keys/timeout/output budget/side-effect class and never invokes a shell.
    - GitReader exposes deterministic root/base/status/history facts with stable ordering and typed unavailable/failure results.
    - Repository text containing shell metacharacters remains one inert argv value.
  </behavior>
  <action>Write injection/timeout/output-budget tests, then implement child-process spawning with `shell: false`, an explicit allowlisted environment, AbortSignal/timeout, bounded capture, and typed exit evidence. GitReader must construct fixed command/argv templates and never pass repository content as an executable/option without separator/validation.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/argv-runner.test.ts</automated></verify>
  <acceptance_criteria>Metacharacter fixtures do not execute; timeout/budget failures are typed; Git facts are deterministically ordered.</acceptance_criteria>
  <done>Git observation has an inspectable no-shell process boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inventory filesystem/Git/package facts without executing the repository</name>
  <reversibility rating="reversible">Observation adapters can broaden behind AnalyzerCapabilities without changing canonical contracts.</reversibility>
  <files>packages/analyzers/package.json, packages/analyzers/src/filesystem/inventory.ts, packages/analyzers/src/git/observation.ts, packages/analyzers/test/no-exec-observation.test.ts, fixtures/no-exec/package.json</files>
  <read_first>@.planning/REQUIREMENTS.md (SEC-001, SEC-002, OBSV-002, OBSV-003, SLICE-010); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md</read_first>
  <behavior>
    - Adapter declares ID/version, supported semantics/languages, EnumerationContract blind spots, and `executesRepositoryCode: false`.
    - Inventory reads manifests/lockfiles/scripts/generated markers/CI/docs/instructions/Git metadata as data with deterministic ordering.
    - A fixture package script that would write a sentinel is never invoked; partial failure produces AnalyzerFailure while unaffected facts remain.
    - No TS/structured-data/Markdown/GitHub-Actions semantic analyzer implementation is present.
  </behavior>
  <action>Write the hostile package fixture and RED sentinel/capability/failure tests, create the analyzer package with a core-only workspace dependency, then implement safe filesystem and Git/package observation through injected RootPath/GitReader ports. Parse only inert manifest data with Zod/JSON, surface unsupported/corrupt lanes explicitly, and enforce the Slice 0 analyzer allowlist in a static test.</action>
  <verify><automated>pnpm vitest run packages/analyzers/test/no-exec-observation.test.ts</automated></verify>
  <acceptance_criteria>Inventory facts/capabilities are deterministic, the sentinel is absent after observation, and unsupported lanes do not erase unaffected facts or prove absence.</acceptance_criteria>
  <done>Phase 1 has the required safe observation wedge and no broad analyzer implementation.</done>
</task>
</tasks>
<prohibitions>- No shell interpolation, repository script/build/test execution, path-prefix-only safety, out-of-root write, or external/network observation. - No broad semantic analyzer implementation.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository paths/content → runtime/analyzers | Hostile paths, metadata, manifests, and Git text enter local observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering | RootPath writes | high | mitigate | Normalize plus drive/UNC/realpath/case/symlink checks before mutation. |
| T-09-02 | Elevation of privilege | Process runner | high | mitigate | `shell: false`, explicit argv/cwd/env/scope/timeout/budget. |
| T-09-03 | Elevation of privilege | Repository content | high | mitigate | Treat as inert data; no-exec sentinel test; content cannot alter policy. |
| T-09-04 | Denial of service | Inventory | medium | mitigate | AbortSignal, scope and output budgets, partial-failure records. |
| T-09-SC | Tampering | Dependencies | high | mitigate | No new install; Node built-ins and verified packages only. |
</threat_model>
<verification>`pnpm vitest run packages/runtime/test/root-path.test.ts packages/runtime/test/argv-runner.test.ts packages/analyzers/test/no-exec-observation.test.ts`</verification>
<success_criteria>Path attacks refuse, Git uses explicit argv, observation cannot execute repository code, and unavailable capabilities stay visible.</success_criteria>
<artifacts_produced>RootPath; argv/Git adapters; filesystem/Git no-exec observers; path/no-exec fixtures.</artifacts_produced>
<output>Create `01-09-SUMMARY.md` when done.</output>
