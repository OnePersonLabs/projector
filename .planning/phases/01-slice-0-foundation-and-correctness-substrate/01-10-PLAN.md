---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 10
type: execute
wave: 7
depends_on: [01-05, 01-08, 01-09]
files_modified:
  - packages/runtime/src/sqlite/migrations.ts
  - packages/runtime/src/sqlite/derived-store.ts
  - packages/runtime/src/rebuild/rebuild-derived-state.ts
  - packages/cli/src/main.ts
  - packages/runtime/test/sqlite-migrations.test.ts
  - packages/runtime/test/rebuild-derived-state.property.test.ts
  - packages/cli/test/canonical-rebuild.acceptance.test.ts
  - fixtures/canonical-rebuild/.gitkeep
autonomous: true
requirements: [PERS-001, PERS-002, PERS-003, SLICE-006, TEST-004, TEST-058, EVAL-004, ACC-001, ACC-063]
estimate: {tokens: 46000, raw_tokens: 46000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "SQLite contains the complete required logical schema but remains fully disposable and derived from canonical/Git/explicit pinned inputs."
    - "Deleting state.db and caches then rebuilding reproduces canonical authored/governance semantics and deterministic root identity."
    - "Rebuild consistency is reported as consistency evidence, not independent correctness proof."
  artifacts:
    - {path: "packages/runtime/src/sqlite/migrations.ts", provides: "Versioned deterministic derived schema migrations"}
    - {path: "packages/runtime/src/rebuild/rebuild-derived-state.ts", provides: "Canonical-to-SQLite rebuild pipeline"}
    - {path: "packages/cli/test/canonical-rebuild.acceptance.test.ts", provides: "ACC-001/ACC-063 black-box closure"}
  key_links:
    - {from: "packages/runtime/src/rebuild/rebuild-derived-state.ts", to: "packages/core/src/storage/canonical-store.ts", via: "validated canonical enumeration", pattern: "CanonicalStore"}
    - {from: "packages/runtime/src/rebuild/rebuild-derived-state.ts", to: "packages/runtime/src/sqlite/derived-store.ts", via: "single SQLite transaction and atomic revision promotion", pattern: "transaction"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Implement the complete derived SQLite schema and prove canonical rebuild closure.

Purpose: Make local queries fast while preserving canonical files as the only authored/governance authority.
Output: Versioned migrations, derived store, rebuild pipeline, property test, and black-box CLI acceptance fixture.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/acceptance-core.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-08-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-09-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Migrate and index one canonical Requirement into disposable SQLite</name>
  <reversibility rating="reversible">SQLite schema is derived and can be dropped/rebuilt; migrations remain versioned for operational continuity.</reversibility>
  <files>packages/runtime/src/sqlite/migrations.ts, packages/runtime/src/sqlite/derived-store.ts, packages/runtime/test/sqlite-migrations.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (PERS-001–003, SLICE-006); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md</read_first>
  <behavior>
    - A fresh DB migrates transactionally to an explicit schema version and indexes one canonical Requirement with source document/hash/revision.
    - All PERS-001 logical table families exist, including derivation inputs, selector dependency keys, capability/failure records, external snapshots, journal, and leases.
    - Opening an incompatible/partial schema fails with typed rebuild-required/corruption status instead of silently altering canonical files.
  </behavior>
  <action>Write RED migration/table/index tests, then implement deterministic prepared-statement migrations and a `DerivedStore` adapter around `node:sqlite`. Use transactions, foreign keys, stable primary keys, explicit schema/engine versions, and no canonical write API. Keep the built-in API contained in runtime and return typed query results.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/sqlite-migrations.test.ts</automated></verify>
  <acceptance_criteria>Fresh/repeated migration is idempotent, every logical family is queryable, and incompatible state requests rebuild without changing canonical inputs.</acceptance_criteria>
  <done>One canonical entity is queryable through a complete disposable derived schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild derived state transactionally from declared inputs</name>
  <reversibility rating="reversible">Rebuild logic can be replaced because SQLite is disposable and canonical inputs remain unchanged.</reversibility>
  <files>packages/runtime/src/rebuild/rebuild-derived-state.ts, packages/runtime/test/rebuild-derived-state.property.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (TEST-004, TEST-058, EVAL-004); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md</read_first>
  <behavior>
    - For fixed repository/Git/canonical/adapter/toolchain/pinned inputs, clean rebuilds produce semantically equivalent indexed authored state and deterministic observations independent of input order.
    - Rebuild never reads live external systems or hidden run history and ignores only declared volatile operational fields.
    - Graph revision promotes atomically only after the rebuild transaction succeeds.
    - Result explicitly labels rebuild consistency and lists independent proof lanes still required.
  </behavior>
  <action>Add fast-check models that shuffle canonical records and delete the DB/cache, then implement the rebuild orchestrator over injected CanonicalStore, analyzers, and DerivedStore. Validate every canonical record before staging, sort all inputs/results, record adapter/toolchain/config digests, stage a new revision in one transaction, and preserve the previous DB on failure.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/rebuild-derived-state.property.test.ts</automated></verify>
  <acceptance_criteria>Repeated clean rebuilds are semantically equal; injected analyzer/store failures do not promote partial revision or modify canonical files.</acceptance_criteria>
  <done>Derived state can be deleted and deterministically reconstructed with truthful evidence labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove complete canonical rebuild closure through the CLI</name>
  <reversibility rating="reversible">The acceptance fixture and CLI orchestration can evolve behind stable command/report contracts.</reversibility>
  <files>packages/cli/src/main.ts, packages/cli/test/canonical-rebuild.acceptance.test.ts, fixtures/canonical-rebuild/.gitkeep</files>
  <read_first>@.planning/REQUIREMENTS.md (ACC-001, ACC-063); @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/acceptance-core.md</read_first>
  <behavior>
    - Fixture authors independently addressable Concept, Requirement, Scenario, Relation, rule, active lens/profile, authority, decision, exception, and migration records.
    - CLI records pre-delete semantic rows/root digest, deletes `state.db` and caches, invokes real rebuild/status, and obtains identical semantics/root digest plus recomputed observations.
    - No monolithic model or hidden run-history file is read.
  </behavior>
  <action>Write the failing black-box acceptance test, author the fixture through public CanonicalStore APIs, then wire CLI status/rebuild to the complete DerivedStore path. Compare normalized canonical/index snapshots and root digest before/after deletion, assert observations were recomputed, and emit both terminal and JSON proof-boundary fields.</action>
  <verify><automated>pnpm vitest run packages/cli/test/canonical-rebuild.acceptance.test.ts</automated></verify>
  <acceptance_criteria>ACC-001 and ACC-063 pass through a spawned CLI process with byte-stable digest and semantic-equivalence assertions.</acceptance_criteria>
  <done>The canonical rebuild closure scenario is executable and visible to a maintainer.</done>
</task>
</tasks>
<prohibitions>- No SQLite canonical writer, monolithic model dependency, live external rebuild read, hidden run-history input, or rebuild-consistency claim presented as independent correctness. - No partial graph revision promotion.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical/Git/analyzer inputs → SQLite | Authoritative and observed records cross into disposable query state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Tampering | SQLite derived state | high | mitigate | Canonical revalidation, prepared statements, transactional staging, disposable rebuild. |
| T-10-02 | Repudiation | Rebuild result | high | mitigate | Before/after canonical digest and normalized semantic comparison; proof-boundary label. |
| T-10-03 | Denial of service | Rebuild | medium | mitigate | AbortSignal, bounded adapters, previous-state preservation, deterministic batching. |
| T-10-SC | Tampering | SQLite dependency | high | mitigate | Use Node built-in behind port; no third-party driver install. |
</threat_model>
<verification>`pnpm vitest run packages/runtime/test/sqlite-migrations.test.ts packages/runtime/test/rebuild-derived-state.property.test.ts packages/cli/test/canonical-rebuild.acceptance.test.ts`</verification>
<success_criteria>Complete logical schema exists; DB deletion/rebuild preserves all canonical semantics/root digest; failure cannot promote partial state.</success_criteria>
<artifacts_produced>Derived SQLite migrations/store; rebuild orchestrator; full canonical-rebuild acceptance fixture.</artifacts_produced>
<output>Create `01-10-SUMMARY.md` when done.</output>
