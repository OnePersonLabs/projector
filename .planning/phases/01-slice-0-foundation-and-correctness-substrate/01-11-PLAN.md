---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 11
type: execute
wave: 6
depends_on: [01-05, 01-06, 01-07, 01-09]
files_modified:
  - packages/core/src/state/binding.ts
  - packages/core/src/state/query-fingerprint.ts
  - packages/core/test/state-binding.property.test.ts
  - packages/core/test/query-fingerprint.property.test.ts
  - packages/core/test/observability-absence.test.ts
  - fixtures/scoped-state-binding/cases.json
autonomous: true
requirements: [PROD-032, KERN-043, KERN-045, KERN-046, KERN-047, ACC-005, ACC-006, ACC-007]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A root digest change alone never stales dependency-scoped work."
    - "Changed bound values or closure-sensitive query results stale/re-evaluate affected work even when old selected entity hashes remain unchanged."
    - "Open, sampled, failed, ambiguous, or unavailable discovery never proves negative-space absence."
  artifacts:
    - {path: "packages/core/src/state/binding.ts", provides: "Pure async StateBinding validator"}
    - {path: "packages/core/src/state/query-fingerprint.ts", provides: "Normalized query/result/dependency digest functions"}
    - {path: "fixtures/scoped-state-binding/cases.json", provides: "ACC-005–007 acceptance matrix"}
  key_links:
    - {from: "packages/core/src/state/binding.ts", to: "packages/core/src/ports/index.ts", via: "StateQueryReader and value reader injection", pattern: "StateQueryReader"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Implement dependency/query-scoped validity and truthful negative-space proof boundaries.

Purpose: Prevent both global false staleness and silently current work after boundary membership changes.
Output: Pure binding validator, query fingerprint logic, scoped-state fixture, and algebraic acceptance properties.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/11-validation/testing-and-adversarial-evaluation.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-05-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-06-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-07-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Rebind after an unrelated root change and stale after a bound value change</name>
  <reversibility rating="costly">Validity semantics control reuse and authorization across the product, though the validator remains behind a stable port.</reversibility>
  <files>packages/core/src/state/binding.ts, packages/core/test/state-binding.property.test.ts, fixtures/scoped-state-binding/cases.json</files>
  <read_first>@.planning/REQUIREMENTS.md (PROD-032, KERN-043, ACC-005); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md</read_first>
  <behavior>
    - Identical snapshot root returns current without rereading dependencies.
    - Changed root plus unchanged explicit values/queries returns rebound with current StateDigest and recomputed dependencyDigest.
    - Any role-appropriate bound value hash change returns stale and lists only changed IDs.
    - Unavailable value comparison returns suspect/unavailable, never current.
  </behavior>
  <action>Write the scoped fixture/property tests first, then implement async validation over injected current-value and StateQueryReader ports. Compare role-appropriate hashes, never add global roots to dependency sets, return deterministic reason ordering, and preserve the original binding on failure.</action>
  <verify><automated>pnpm vitest run packages/core/test/state-binding.property.test.ts</automated></verify>
  <acceptance_criteria>ACC-005 passes; unrelated changes rebind and bound changes stale with exact changed-ID evidence.</acceptance_criteria>
  <done>Local work validity is independent from unrelated repository churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fingerprint closure-sensitive query membership and program identity</name>
  <reversibility rating="costly">Query projection/program version semantics determine stale-work correctness and cache compatibility.</reversibility>
  <files>packages/core/src/state/query-fingerprint.ts, packages/core/test/query-fingerprint.property.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-045, KERN-046, ACC-006); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md</read_first>
  <behavior>
    - Query hash covers program ID/version and normalized serializable input.
    - Result hash deterministically covers identity, membership, existence, closure-relevant ranks/qualifiers/declared semantic properties and excludes display metadata.
    - New matching entity/relation/membership changes result fingerprint and stales even when all prior entity hashes are unchanged.
    - Query program/version or declared result projection changes invalidate; uncertain dependency keys force rerun.
  </behavior>
  <action>Add fast-check membership/order/program-version properties, then implement normalized query/result/dependency digest functions and conservative dependency-key impact logic. Empty sets remain real fingerprints when correctness depends on them; dependencyDigest includes full normalized value/query/result records.</action>
  <verify><automated>pnpm vitest run packages/core/test/query-fingerprint.property.test.ts</automated></verify>
  <acceptance_criteria>ACC-006 passes and no display-only change perturbs a result hash unless declared closure-relevant.</acceptance_criteria>
  <done>Boundary membership cannot change without re-evaluation or staleness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refuse absence proof outside closed or valid bounded lanes</name>
  <reversibility rating="reversible">The proof predicate is a pure policy function behind stable observability contracts.</reversibility>
  <files>packages/core/src/state/binding.ts, packages/core/test/observability-absence.test.ts, fixtures/scoped-state-binding/cases.json</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-047, ACC-007); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/state-binding-and-ports.md</read_first>
  <behavior>
    - Empty closed results may prove absence.
    - Empty bounded results prove absence only while every declared assumption is verified current.
    - Open, sampled, unavailable, failed, or assumption-invalid results return suspect/unavailable and widen reasons/frontier.
  </behavior>
  <action>Write table-driven RED cases across every observability class and assumption state, then implement the negative-space proof predicate and integrate it into binding validation. Return explicit proof boundary and reasons; ranking relevance is allowed but must remain distinct from absence proof.</action>
  <verify><automated>pnpm vitest run packages/core/test/observability-absence.test.ts</automated></verify>
  <acceptance_criteria>ACC-007 passes for open/sampled/unavailable lanes and closed/bounded positive controls demonstrate the exact allowable proof cases.</acceptance_criteria>
  <done>Unavailable/open-world discovery cannot silently authorize a current/completion claim.</done>
</task>
</tasks>
<prohibitions>- No global-root-only invalidation, omitted query dependencies, dependency-key correctness shortcut, executable query, silent current on failure, or open-world absence proof.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Changed repository/query observations → StateBinding validator | New or unavailable facts can revoke reuse and authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering | Binding reuse | high | mitigate | Value plus query comparison and conservative rerun on uncertain dependency keys. |
| T-11-02 | Elevation of privilege | Negative-space authorization | high | mitigate | Observability/assumption proof predicate; suspect/unavailable states. |
| T-11-03 | Repudiation | Validation reasons | medium | mitigate | Deterministic changed IDs/query IDs/reasons and rebound binding. |
| T-11-SC | Tampering | Dependencies | high | mitigate | No new install; canonical hashing/fast-check from verified lock. |
</threat_model>
<verification>`pnpm vitest run packages/core/test/state-binding.property.test.ts packages/core/test/query-fingerprint.property.test.ts packages/core/test/observability-absence.test.ts`</verification>
<success_criteria>ACC-005/006/007 pass, proving scoped rebind, membership staleness, and observability-bounded absence.</success_criteria>
<artifacts_produced>Binding validator; query/result fingerprints; observability proof predicate; scoped-state fixture/properties.</artifacts_produced>
<output>Create `01-11-SUMMARY.md` when done.</output>
