---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 12
type: execute
wave: 8
depends_on: [01-08, 01-10]
files_modified:
  - packages/runtime/src/journal/transaction-journal.ts
  - packages/runtime/src/journal/writer-lease.ts
  - packages/runtime/src/journal/recovery.ts
  - packages/core/src/storage/canonical-store.ts
  - packages/runtime/src/rebuild/rebuild-derived-state.ts
  - packages/runtime/test/transaction-journal.test.ts
  - packages/runtime/test/writer-lease.test.ts
  - packages/runtime/test/transaction-crash.acceptance.test.ts
  - fixtures/transaction-crash/runner.ts
autonomous: true
requirements: [SLICE-007, ACC-008]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Exactly one writer lease protects canonical/derived promotion and expired ownership cannot silently overwrite an active writer."
    - "Every multi-document mutation records prepared → workspace mutation → staged → validating → canonical staging → commit/rollback phases."
    - "Restart after forced process death resumes, rolls back, or reports recovery-required; canonical completion never coexists with partial workspace state."
  artifacts:
    - {path: "packages/runtime/src/journal/transaction-journal.ts", provides: "Durable transaction phase journal"}
    - {path: "packages/runtime/src/journal/recovery.ts", provides: "Deterministic restart decision matrix"}
    - {path: "packages/runtime/test/transaction-crash.acceptance.test.ts", provides: "ACC-008 forced-interruption gate"}
  key_links:
    - {from: "packages/core/src/storage/canonical-store.ts", to: "packages/runtime/src/journal/transaction-journal.ts", via: "injected transaction coordinator", pattern: "journal"}
    - {from: "packages/runtime/src/journal/recovery.ts", to: "packages/runtime/src/rebuild/rebuild-derived-state.ts", via: "post-recovery derived rebuild", pattern: "rebuild"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Make fine-grained canonical mutation crash-consistent with a writer lease, durable journal, and forced-interruption recovery.

Purpose: Preserve atomic semantic truth across physically independent files and disposable SQLite revisions.
Output: Lease/journal/recovery runtime, store/rebuild integration, and transaction-crash acceptance harness.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/acceptance-core.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-08-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-10-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Acquire one writer lease and journal one empty semantic transaction</name>
  <reversibility rating="costly">Journal/lease durable record versions affect recovery of in-flight repositories, but are explicitly versioned.</reversibility>
  <files>packages/runtime/src/journal/transaction-journal.ts, packages/runtime/src/journal/writer-lease.ts, packages/runtime/test/transaction-journal.test.ts, packages/runtime/test/writer-lease.test.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (SLICE-007); @.planning/intel/source-snapshot/PROJECTOR_SPEC/09-evolution/persistence-and-observation.md</read_first>
  <behavior>
    - Lease acquisition is exclusive and binds owner/run/state/expiry/heartbeat using atomic filesystem/SQLite coordination.
    - Journal append and phase transition are durable, ordered, idempotent, and reject illegal transitions.
    - Empty transaction reaches committed with one graph revision promotion and compact evidence.
  </behavior>
  <action>Write failing lease contention and journal-state-machine tests, then implement versioned records, atomic acquire/renew/release, legal phase transitions, fsync/durable persistence, and deterministic typed failures. Bind lease and journal to starting StateDigest; never use wall-clock timestamp as semantic identity.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/transaction-journal.test.ts packages/runtime/test/writer-lease.test.ts</automated></verify>
  <acceptance_criteria>Concurrent acquisition yields one owner, illegal/stale transitions refuse, and an empty transaction commits exactly once.</acceptance_criteria>
  <done>One writer can execute and prove an empty semantic transaction safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover deterministically from every journal phase</name>
  <reversibility rating="reversible">Recovery algorithms can evolve behind the versioned journal contract.</reversibility>
  <files>packages/runtime/src/journal/recovery.ts, packages/runtime/test/transaction-crash.acceptance.test.ts, fixtures/transaction-crash/runner.ts</files>
  <read_first>@.planning/REQUIREMENTS.md (ACC-008); @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/acceptance-core.md (Crash recovery matrix)</read_first>
  <behavior>
    - A child process can be killed after prepared, workspace mutation, staged, validating, canonical staging, commit, and rollback.
    - Restart chooses safe resume, rollback, or typed recovery-required from durable evidence.
    - No outcome reports completed when workspace/canonical/SQLite state is partial; repeated recovery is idempotent.
  </behavior>
  <action>Build the crash runner to signal the parent after durable phase transition and terminate abruptly on command, then implement a recovery decision matrix using journal, staged hashes, Git/workspace state, canonical hashes, and derived revision. Test empty and sample multi-document transactions across every phase with a fresh process.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/transaction-crash.acceptance.test.ts</automated></verify>
  <acceptance_criteria>ACC-008 passes for every phase; each restart converges or reports recovery-required without a false completion claim.</acceptance_criteria>
  <done>Forced process death is detected/recovered across the full journal phase matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route canonical update and derived promotion through the coordinator</name>
  <reversibility rating="reversible">Coordinator injection preserves CanonicalStore/DerivedStore public contracts.</reversibility>
  <files>packages/core/src/storage/canonical-store.ts, packages/runtime/src/rebuild/rebuild-derived-state.ts, packages/runtime/test/transaction-crash.acceptance.test.ts</files>
  <read_first>@packages/core/src/storage/canonical-store.ts; @packages/runtime/src/rebuild/rebuild-derived-state.ts; @packages/runtime/src/journal/recovery.ts</read_first>
  <behavior>
    - Canonical multi-document changes require a lease/journal coordinator and record before/after hashes/touched IDs.
    - SQLite graph revision promotes only after canonical commit reaches its allowed phase.
    - Validation failure rolls back/stages recovery evidence and never overwrites the prior valid canonical/derived snapshot.
  </behavior>
  <action>Extend store/rebuild injection points to require the transaction coordinator for writes and revision promotion. Add crash cases covering a real canonical update plus derived reindex and assert the previous canonical/DB snapshot remains recoverable on validation failure.</action>
  <verify><automated>pnpm vitest run packages/runtime/test/transaction-crash.acceptance.test.ts -t "canonical update|derived promotion"</automated></verify>
  <acceptance_criteria>All mutation/promotions are journaled and no direct uncoordinated write path remains in the public store/rebuild APIs.</acceptance_criteria>
  <done>Crash consistency protects real canonical and derived operations, not only the empty tracer.</done>
</task>
</tasks>
<prohibitions>- No lockfile-by-convention without atomic ownership, unjournaled canonical multi-write, partial revision promotion, completion from process exit alone, or automatic overwrite of ambiguous recovery state.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent/crashed process → canonical and derived promotion | Writer ownership and journal evidence determine whether mutation may resume or commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Tampering | Writer lease | high | mitigate | Atomic exclusive ownership, state binding, heartbeat/expiry evidence, contention tests. |
| T-12-02 | Repudiation | Transaction completion | high | mitigate | Durable ordered journal, before/after hashes, legal-transition enforcement. |
| T-12-03 | Denial of service | Crashed writer | medium | mitigate | Deterministic resume/rollback/recovery-required matrix and idempotent restart. |
| T-12-SC | Tampering | Dependencies | high | mitigate | No new package install; use fs/SQLite/Git seams already verified. |
</threat_model>
<verification>`pnpm vitest run packages/runtime/test/transaction-journal.test.ts packages/runtime/test/writer-lease.test.ts packages/runtime/test/transaction-crash.acceptance.test.ts`</verification>
<success_criteria>Writer contention, illegal transitions, every crash phase, real canonical update, and derived promotion pass without false completion.</success_criteria>
<artifacts_produced>Writer lease; journal state machine; recovery coordinator; crash process harness and acceptance matrix.</artifacts_produced>
<output>Create `01-12-SUMMARY.md` when done.</output>
