---
phase: 01-slice-0-foundation-and-correctness-substrate
plan: 13
type: execute
wave: 9
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08, 01-09, 01-10, 01-11, 01-12]
files_modified:
  - packages/core/src/bootstrap/reference-stack.ts
  - packages/cli/src/main.ts
  - packages/core/package.json
  - packages/runtime/package.json
  - packages/analyzers/package.json
  - packages/cli/package.json
  - packages/testkit/package.json
  - packages/testkit/test/package-graph.test.ts
  - packages/testkit/test/phase-1.acceptance.test.ts
  - scripts/check-package-graph.ts
autonomous: true
requirements: [KERN-051, KERN-052, KERN-053, SLICE-005, ACC-009]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Core has no workspace dependency; runtime/analyzers depend only on core; CLI is the sole concrete composition root."
    - "Core services run with in-memory fakes and import no filesystem, SQLite, Git process, host, model vendor, or CLI implementation."
    - "Reference stack choices, rationale, and reconsideration triggers materialize as canonical decision/authority records on init."
    - "The complete Phase 1 CLI and acceptance suite pass from a clean install/rebuild."
  artifacts:
    - {path: "scripts/check-package-graph.ts", provides: "Static dependency-direction gate"}
    - {path: "packages/core/src/bootstrap/reference-stack.ts", provides: "Canonical reference-stack decision/authority records"}
    - {path: "packages/testkit/test/phase-1.acceptance.test.ts", provides: "Phase-wide CLI/property/security/recovery gate"}
  key_links:
    - {from: "packages/cli/src/main.ts", to: "packages/core/src/ports/index.ts", via: "composition of runtime/analyzer implementations", pattern: "create.*Store|create.*Analyzer"}
    - {from: "packages/cli/src/main.ts", to: "packages/core/src/bootstrap/reference-stack.ts", via: "init canonical bootstrap transaction", pattern: "referenceStack"}
---

## Phase Goal

**As a** Projector maintainer, **I want to** initialize and safely operate rebuildable fine-grained canonical semantic state, **so that** deterministic contracts and scoped validity can support every governed change slice.

<objective>
Lock the ports architecture, materialize reference-stack governance, and run the complete Phase 1 gate.

Purpose: Prove the repository is ready for the misplaced-script loop without cycles, concrete dependencies in core, ungoverned stack choices, or missing acceptance evidence.
Output: Package-graph checker, canonical stack decision/authority bootstrap, final composition wiring, and full Phase 1 acceptance test.
</objective>
<execution_context>@/home/zethj/dev/projector/.codex/gsd-core/workflows/execute-plan.md; @/home/zethj/dev/projector/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md; @.planning/REQUIREMENTS.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/implementation-plan.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-01-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-05-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-08-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-10-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-11-SUMMARY.md; @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-12-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Fail and then pass the exact workspace dependency-direction gate</name>
  <reversibility rating="costly">Package-direction changes affect every import, but the rule is repository-internal and can be changed by a governed architecture decision.</reversibility>
  <files>scripts/check-package-graph.ts, packages/testkit/test/package-graph.test.ts, packages/core/package.json, packages/runtime/package.json, packages/analyzers/package.json</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-051, KERN-052, SLICE-005, ACC-009); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md</read_first>
  <behavior>
    - Core has no workspace dependency and no concrete adapter/runtime/host/provider/persistence imports.
    - Runtime/analyzers depend on core only; CLI may depend on core/runtime/analyzers; testkit may depend on public package facades for tests.
    - The checker encodes `engine|integrations → core` and `cli → core + engine + analyzers + runtime + integrations` as the allowed reference graph when those dependency-justified packages exist, without requiring speculative package creation in Phase 1.
    - Deep/internal imports, dependency cycles, undeclared imports, and forbidden Node APIs in core fail with import-edge evidence.
  </behavior>
  <action>Write the package-graph test with a deliberately invalid fixture edge and confirm RED, then implement a deterministic checker over manifests plus TypeScript import graphs. Repair package exports/dependencies to the reference direction, permit no speculative package, and report all violations sorted by package/file/import.</action>
  <verify><automated>pnpm vitest run packages/testkit/test/package-graph.test.ts &amp;&amp; node scripts/check-package-graph.ts</automated></verify>
  <acceptance_criteria>ACC-009 passes for the real workspace and the injected reverse/deep/core-concrete edges fail with exact diagnostics.</acceptance_criteria>
  <done>The ports architecture and dependency direction are executable CI gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize reference stack decisions and reconsideration triggers during init</name>
  <reversibility rating="costly">Changing defaults affects many consumers, but explicit canonical decisions/triggers make the change governable.</reversibility>
  <files>packages/core/src/bootstrap/reference-stack.ts, packages/cli/src/main.ts, packages/cli/package.json, packages/testkit/package.json</files>
  <read_first>@.planning/REQUIREMENTS.md (KERN-053); @.planning/intel/source-snapshot/PROJECTOR_SPEC/02-semantic-kernel/reference-implementation.md (Technology choices)</read_first>
  <behavior>
    - Init writes versioned canonical Architecture Decision/Authority records for Node 24, strict TS/ESM, pnpm, Zod/JSON Schema, SQLite derived state, TypeScript Compiler API, source-location-preserving structured-data/Markdown parsers, Git subprocess, Vitest/fast-check, canonical JSON/SHA-256, and JSONL/optional OTel-compatible spans.
    - Each record contains the specified rationale and reconsideration triggers; repeated init is idempotent.
    - Records are written through CanonicalStore/journal and indexed by rebuild, never hardwired as uninspectable runtime truth.
  </behavior>
  <action>Write the failing init fixture assertions, then implement pure constructors for deterministic decision/authority records and wire them through the canonical transaction on init. Derive engine/toolchain version evidence from the lock/runtime, preserve the fixed choices, and make later changes require new governed records rather than silent code edits.</action>
  <verify><automated>pnpm vitest run packages/testkit/test/phase-1.acceptance.test.ts -t "reference stack"</automated></verify>
  <acceptance_criteria>Fresh/repeated init produces the same canonical records/hashes and every technology choice has rationale plus a typed reconsideration trigger.</acceptance_criteria>
  <done>Reference technology defaults are inspectable canonical governance information.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute the complete Phase 1 acceptance gate through the CLI</name>
  <reversibility rating="reversible">The aggregate test harness can evolve while individual contracts remain stable.</reversibility>
  <files>packages/testkit/test/phase-1.acceptance.test.ts, packages/cli/src/main.ts</files>
  <read_first>@.planning/ROADMAP.md (Phase 1 success criteria); @.planning/phases/01-slice-0-foundation-and-correctness-substrate/01-SOURCE-AUDIT.md; @.planning/intel/source-snapshot/PROJECTOR_SPEC/12-delivery/implementation-plan.md</read_first>
  <behavior>
    - Clean install/typecheck/test runs with exact lock; real CLI init/status terminal+JSON works.
    - Registry closure, canonical locality/hash/root order, path/no-exec, SQLite rebuild closure, scoped binding, open-world refusal, crash recovery, and package direction all run.
    - A static boundary assertion confirms no broad analyzer, host/provider, web UI, daemon, or external adapter implementation entered Phase 1.
    - Report lists proof lane and any unavailable/unknown boundary instead of claiming universal correctness.
  </behavior>
  <action>Write an aggregate acceptance test that invokes public commands/tests rather than reimplementing their assertions. Complete the CLI composition root by injecting final canonical, SQLite, analyzer, binding, and journal adapters; ensure terminal and JSON status expose the same deterministic digests, capability failures, recovery state, and proof-boundary labels. Run the entire suite from a fresh fixture and inspect the resulting diff before commit per DELV-003.</action>
  <verify><automated>pnpm install --frozen-lockfile --ignore-scripts &amp;&amp; pnpm -r exec tsc --noEmit &amp;&amp; pnpm -r test &amp;&amp; node scripts/check-package-graph.ts</automated></verify>
  <acceptance_criteria>All Phase 1 success criteria and acceptance fixtures pass from the locked workspace; output is deterministic and no committed requirement is represented as deferred work.</acceptance_criteria>
  <done>The complete Phase 1 correctness substrate is executable, inspectable, and ready for Phase 2.</done>
</task>
</tasks>
<prohibitions>- No engine/runtime/analyzer cycle, concrete adapter import in core, deep internal import, speculative package, broad analyzer, host/provider integration, web UI, daemon, or false universal-completion claim. - No silent stack-choice change.</prohibitions>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Manifests/import graph and stack metadata → composition/final gate | Dependency or governance drift can bypass architecture and completion claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Elevation of privilege | Package dependency graph | high | mitigate | Static manifest/import graph gate blocks reverse/deep/concrete-core edges. |
| T-13-02 | Tampering | Reference stack governance | medium | mitigate | Canonical decision/authority records with hashes, evidence, triggers, journaled init. |
| T-13-03 | Repudiation | Phase completion | high | mitigate | Aggregate acceptance references independent behavior gates and reports proof boundaries. |
| T-13-SC | Tampering | Frozen install | high | mitigate | Exact verified lock, frozen/script-disabled install, package graph and full suite. |
</threat_model>
<verification>`pnpm install --frozen-lockfile --ignore-scripts &amp;&amp; pnpm -r exec tsc --noEmit &amp;&amp; pnpm -r test &amp;&amp; node scripts/check-package-graph.ts`</verification>
<success_criteria>Reference dependency graph passes, stack decisions are canonical, all Phase 1 acceptance tests pass, and the CLI reports truthful deterministic state.</success_criteria>
<artifacts_produced>Package-graph checker; reference-stack governance bootstrap; final composition wiring; aggregate Phase 1 acceptance gate.</artifacts_produced>
<output>Create `01-13-SUMMARY.md` when done.</output>
