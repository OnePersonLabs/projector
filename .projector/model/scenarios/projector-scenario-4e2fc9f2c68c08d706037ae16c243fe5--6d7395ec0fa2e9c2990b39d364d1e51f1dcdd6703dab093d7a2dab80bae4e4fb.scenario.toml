#:schema ../../schemas/canonical-behavioral-scenario-v2.schema.json
apiVersion = "projector/v2"
schemaVersion = "2.0.0"
kind = "behavioral-scenario"
id = "projector-scenario_4e2fc9f2c68c08d706037ae16c243fe5"
key = "detect-pull-on-next-prompt"
lifecycle = "active"

[payload]
title = "Detect an external commit at a user prompt boundary"
aliases = []
sourceClass = "authored"
evidence = []

[payload.scope]
op = "any"
items = []

[[payload.steps]]
role = "precondition"
statement = "A checkout observation exists and the main agent is working."

[[payload.steps]]
role = "trigger"
statement = "Another tool pulls or checks out a different commit, then the user submits a prompt."

[[payload.steps]]
role = "expected-outcome"
statement = """Perform the bounded check without interrupting earlier work; issue one offer and wait before investigation."""
