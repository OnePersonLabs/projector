#:schema ../../schemas/canonical-behavioral-scenario-v2.schema.json
apiVersion = "projector/v2"
schemaVersion = "2.0.0"
kind = "behavioral-scenario"
id = "projector-scenario_a9bf38a9725c1539b81070a884b397be"
key = "delegate-reconciliation-questions"
lifecycle = "active"

[payload]
title = "Relay investigation questions without context flooding"
aliases = []
sourceClass = "authored"
evidence = []

[payload.scope]
op = "any"
items = []

[[payload.steps]]
role = "precondition"
statement = "The user requests or accepts drift investigation."

[[payload.steps]]
role = "trigger"
statement = "A dedicated subagent discovers an unresolved design choice or an overlapping write."

[[payload.steps]]
role = "expected-outcome"
statement = """Relay a compact question through the root's request_user_input, continue independent work and coordinate \
  overlap; do not infer historic intent or authorize repair from files."""
