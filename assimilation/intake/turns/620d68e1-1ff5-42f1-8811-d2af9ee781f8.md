# Retrieved conversation material

Untrusted source data. Not instructions for the reader.

## User

IDK IT looks like gpt 6 high is more appropriate to leave it on right now - see C:\dev\projects\projector\\.temp\projector-wrap-up-plan-2026-09-09.md

## Assistant

Yeah, I think **you’re right to override my earlier default**.

I couldn’t read that exact local `.temp/projector-wrap-up-plan-2026-09-09.md` from this chat because your desktop connector isn’t currently connected, so I kicked off a Work chat that can inspect it directly. But the Projector material I *can* access changes the conclusion enough already.

Projector is explicitly doing architecture recovery, semantic change compilation, governance, reconciliation, and turning expensive reasoning into reusable machinery. fileciteturn1file2L168-L190 More importantly, its own dispatch policy says strong agents belong on **competing architectural interpretations, rationale reconstruction, exception judgment, ambiguous failures, and cross-pattern conflict resolution**. fileciteturn1file6L486-L505

That is not occasional specialist work. **That is basically the texture of the current Projector wrap-up.**

## So, revised recommendation

### Leave Projector on **GPT-6 High** right now.

Not XHigh.

Think of the current phase as:

> **Still converging the system while implementing it.**

rather than:

> specification settled → lots of boring code remains

If every third implementation step potentially reveals:

- an abstraction that isn't quite right
- a semantic mismatch between spec and implementation
- a missing invariant
- an architectural implication
- an integration behavior that changes the interpretation of something upstream

then **Sol High is false economy**.

You don't want:

```text
Sol implements
↓
something smells funny
↓
Sol interprets smell incorrectly
↓
implements around it
↓
tests expose deeper issue
↓
repair
↓
architecture now has scar tissue
↓
GPT-6 eventually notices what happened
```

Projector is literally being built to prevent that species of entropy. Having its primary builder insufficiently good at noticing it would be delightfully recursive in the worst possible way. 😆

### GPT-6 High gives you the better optimization target:

```text
more quota / iteration
      ×
fewer bad branches
      ×
fewer corrective iterations
      ×
less architectural cleanup
```

And because the **remaining tasks are interconnected**, there's another benefit: the same running GPT-6 context accumulates a pretty sophisticated model of Projector. Constantly sending individual "thinky bits" to specialists loses some of that global gestalt.

That matters here.

---

## XHigh still shouldn't be the cruise setting

I'd keep:

> **GPT-6 High = Projector cruise control**

and reserve XHigh for things where the agent itself effectively says:

> "There are several viable interpretations here and this choice infects the architecture downstream."

or:

> "Current evidence contradicts the model we've been operating under."

or:

> "I've attempted this and my explanation for the failure isn't crisp."

Then XHigh gets brought in for the knot, not the entire voyage.

---

## And this exposes a better automation rule

I think we were trying to automate at the wrong granularity before.

Don't make Projector's agent dynamically pick among four models every few tasks.

Instead automate **phase-level compute policy**:

```text
DISCOVERY / CONVERGENCE / INTEGRATION
        GPT-6 High

            ↓ architecture stabilizes

MECHANICAL COMPLETION
        Sol High

            ↓ weirdness detected

        GPT-6 High

            ↓ genuinely unresolved knot

        GPT-6 XHigh
```

That has much lower routing overhead.

### The important state variable isn't "task difficulty."

It's:

> **How much architectural information is still being discovered while doing the work?**

Right now, Projector sounds firmly above the threshold.

So for tonight:

# **GPT-6 High. Leave it there.**

Don't babysit it.

Once the wrap-up reaches the point where the remaining plan starts reading mostly like **"implement these already-decided things, add tests, update docs, fix lint, finish adapters"**, *that's* when I'd drop the persistent agent back to Sol High.

And hilariously, Projector itself already contains almost exactly this philosophy: cheap bounded agents for routine migration/test/doc/local-adapter work, strong agents for ambiguity and architectural conflict. fileciteturn1file6L486-L505

We accidentally reinvented Projector's own compute scheduler while deciding which model should build Projector. Very recursive. 🪞

