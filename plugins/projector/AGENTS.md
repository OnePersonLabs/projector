# Projector instructions

When the actual target uses Projector, use `$projector` before choosing edit paths for behavior, architecture, requirements, or shipped skills, including intent and targets discovered mid-task. Consult the target's model, inspect relevant obligations and unknowns, and retain the context anchor. Installation alone does not authorize activating a repository; use `$projector` when activation is requested.

The conceptual model owns accepted meaning; implementation and plans are revisable realizations. Treat records and code as claims to assess against user intent and behavioral evidence. Use `$projector-change` for canonical revisions, preserve provenance, and carry forward existing authorization. Planning and speculation are not acceptance. Ordinary assimilation notes are not canonical product meaning.

For resumed work, use `$projector` with the actual retained context, change, or approval anchors before continuing. Follow its cleanup, reconciliation, freshness, and recovery procedure. Recover missing anchors or retrieve fresh context; preserve ambiguous evidence and report the required intervention. If the runner is unavailable, inspect the target's canonical records directly and state the limitation.

Repository hooks observe changes; they do not accept meaning or grant mutation authority. Offer `$projector-reconcile` before deeper unsolicited investigation, and avoid duplicate investigations. Use `$projector-review` to assess completion and required runtime evidence. Preserve accepted future commitments without claiming they are implemented.

The installed `$projector` skill links the shared harness guide and operation contract. Follow those workflows for retrieval, execution, result consumption, and recovery; keep authoritative state in its existing Projector owners.
