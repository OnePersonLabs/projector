# Projector control-loop verification, 2026-09-13

This record preserves the 2.1.7 checkout and installed Windows bundle exercises, followed by later checkout evidence. It is evidence for the existing canonical obligations and [harness guide](../plugins/projector/references/harness-guide.md), not a new progress owner or a claim of global completion.

| Exercise | Observed result | Assurance and limit |
| --- | --- | --- |
| `pnpm verify` after the canonical and initial skill changes; later focused plugin, bundle, and continuation tests | Typecheck, default tests, boundaries, and specification check passed. The focused follow-up passed 18 tests across the plugin hook, standalone bundle, and continuation suites. The final skill ordering was checked with a fresh installed-host run. | Covers deterministic contracts and source-severed package assembly. The default suite does not run a paid agent host or live browser case. |
| Approved model-only lifecycle transactions | The existing runner decision, evidence-bound completion requirement, and application scenario were revised together; the reuse-after-reset scenario was revised in a second selected context. Both approved lifecycles report authenticated historical completion through `cleanup`. | Shows canonical provenance and exact-plan mutation through the installed operation runner. A completed transaction does not establish every runtime workflow. |
| Access recovery regression in the default suite | Dead-process expired claims and the recognized abandoned mutex can be reclaimed; live and ambiguous claims remain protected. | Validates selector-free operation-access recovery separately from approved-change recovery. No live process was killed for this exercise. |
| Three fresh Codex processes on a nested initialized fixture | The first selected `$projector`, activated the fixture, persisted context, and reported absent accepted checker meaning. After an out-of-band source edit, a fresh process reconciled and found an unpredicted source impact. That process skipped `cleanup`, exposing an instruction ordering gap. After the skill was reordered and reinstalled, another fresh process sent `cleanup` with the retained context before `reconcile`; cleanup returned current context, one unknown evidence item, and a `complete` next action. | The fixture was under the Projector checkout and could inherit its root AGENTS.md. This demonstrates the observed operations and result consumption, but does not independently establish consumer instruction delivery. It does not exercise an agent-initiated controlled write or prove every future agent will obey the skill. The skipped-cleanup observation motivated the final instruction change. |
| Installed `application.observe` on an isolated Psychord clone with pinned `agent-browser` 0.31.1 | The no-input case published a current, passed artifact. All 13 recorded assertions passed, and the browser context and server process were released. A second plan with a deliberately wrong build hash published an unavailable result with failed build and served-byte assertions; both resources were released. | Exercises the installed operation entry, real Windows Chrome, published artifact consumption, source/build pin checking, and failure cleanup for one supported case. The plan also pinned development-checkout controller/helper files; this run alone does not prove those plan dependencies are available when the checkout is absent. The separate source-severed bundle test checks packaging isolation, not this browser plan. |

The original Psychord repository was not upgraded for this exercise. Its installed readiness was `upgrade-required`, so the live operation ran in a disposable clone with its owned evidence copied for an isolated project-data migration. A real-host service test first reported unavailable because the global `agent-browser` was 0.37.1 while this adapter pins 0.31.1; using an isolated 0.31.1 executable made the no-input test pass. The version mismatch was explicit and did not count as behavioral evidence.

Remaining uncertainty: no fresh installed-agent session was interrupted during a controlled write, although deterministic lifecycle and continuation tests cover the journal phases; only one Psychord browser case and one negative build pin were run through the installed entry. Broader application adapters and other explicitly unrealized canonical commitments remain outside the installed operation map. Their accepted status is not evidence of implementation.

## Independent instruction delivery and continuation

The repository root AGENTS.md now routes Projector development instructions. Consumer baseline instructions live in `plugins/projector/AGENTS.md`; the separate `projector-instructions.mjs` SessionStart command reads that installed file directly and emits it completely for startup, resume, clear, and compact. The observation hook remains independent. The existing runner decision records this ownership through change `semantic_change_0e1c4ca04227c350e50946f860228c5d`, approval `lifecycle_approval_64a9128317ac33db5dd852b128486482`; canonical apply and its model-baseline validations succeeded without asserting runtime conformance.

- Installed-copy tests compare the complete injected contents, exercise all four sources outside Git and in an inactive repository, break the observation runtime, and check explicit missing/unreadable instruction-file errors. The release packaging test also executes injection from its assembled bundle outside a repository. Both affected suites passed in the full run. One separate packaging run timed out while Windows/WSL preparation overlapped; its later full-suite execution passed.
- The refresh helper rebuilt and installed 2.1.7 in the normal Windows and WSL Codex homes and verified all four hook commands were trusted. Installed Windows instruction, hook, and manifest bytes match source.
- Fresh Codex sessions ran in `C:/Users/zethj/AppData/Local/Temp/projector-independent-284f190937`, outside the checkout. The fixture and every ancestor were checked for AGENTS.md, AGENTS.override.md, and CLAUDE.md; none existed. The user-level Codex AGENTS.md contained no Projector instruction. Both session transcripts contain the entire new plugin AGENTS.md in a developer message, establishing actual injection rather than relying on file presence.
- The first natural request asked to activate Projector and assess the link checker without editing implementation. The agent selected `$projector`, initialized, retrieved and consumed context, ran the existing tests, and distinguished observed code from absent accepted requirements. Anchor: `knowledge_context_79d67830297a8eb51cc3681604462e88`; session `01a09c1a-33e4-7df1-9799-f834db4510f2`.
- An external edit added `trimStart()` before prefix matching. A separate fresh session received only the retained context ID and a natural request to reassess. It selected `$projector` and `$projector-reconcile`, called cleanup before reconciliation, retrieved fresh context, and investigated current behavior. The fixture has no initial Git commit or retained old source, so exact prior behavior is not recoverable from its stored context. The agent explicitly preserved that uncertainty instead of treating current discovery as behavioral proof. Session: `01a09c1b-e7c6-7a73-b449-234a126aa830`. These paid-host exercises remain outside the default suite.

Final instruction review used `$opl:agent-instructions`: baseline ownership, workflow routing, recovery instructions, installed paths, and independent hook delivery have no unresolved findings. No skill invocation metadata changed in this split.

The 2.1.7 `pnpm verify` run was **not green**: 1,140 tests passed, but `source-severed-release-bundle.test.ts` rejected a stale migration seal because compiled format owners no longer matched the sealed target. Plugin instruction and hook files were excluded from that snapshot. This is historical evidence for that checkout state, not the current release result.

The working tree subsequently advanced to 2.1.8 and already contained the 2.1.7-to-2.1.8 migration, ordered chain, and matching format target when the release failure was revisited. The previously failing source-severed test passed alone and again in the full `pnpm verify` run. That run passed typecheck, 149 test files with 1,213 passing tests, package boundaries, and specification checking. The exact same-target chain seal is therefore validated for the current compiled candidate. This follow-up verified the existing 2.1.8 repair; it did not create the migration or establish release readiness for future source changes.

## Bounded observation and disposable cache delivery

The subsequent 2.1.9 checkout introduced complete-or-fail bounded repository observation, compatible snapshot comparison, protected disposable-cache collection, and the release-workflow timeout and cleanup repairs. After the last implementation edit, the default `pnpm verify` completed successfully with typechecking, 149 passing test files, 1,236 passing tests, package-boundary checks, and specification checking. Ten existing real-environment tests were skipped. The source-severed release integration passed in the default parallel run within its 30-second outer limit. These checks establish the tested checkout behavior, not a paid-host or browser-wide conformance claim.

The separate `pnpm release:artifacts:check` remains unavailable as a release-readiness sign-off: its traceability map lacks nine obligation IDs already present in the committed canonical baseline. The bounded-observation work neither created those canonical IDs nor establishes their release ownership. That mapping requires its own scoped investigation before treating the artifact check as green.
