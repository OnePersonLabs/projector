{
  "version": 1,
  "request": "Trim surrounding label whitespace while preserving existing callers.",
  "proposalPath": "change-proposal.json",
  "expectedPaths": [
    "src/format-label.mjs",
    "test/trim-label.test.mjs"
  ],
  "beforeSource": "export const formatLabel = (value) => String(value);\n",
  "afterSource": "export const formatLabel = (value) => typeof value === 'string' ? value.trim() : String(value);\n",
  "indexSource": "export { formatLabel } from './format-label.mjs';\n",
  "supplementalTestSource": "import assert from 'node:assert/strict'; import { formatLabel } from '../src/index.mjs'; assert.equal(formatLabel('  beta  '), 'beta');\n",
  "independentOracleSource": "import assert from 'node:assert/strict';\nimport { existsSync } from 'node:fs';\nimport { setTimeout as delay } from 'node:timers/promises';\nimport { formatLabel } from '../src/index.mjs';\nif (existsSync('.projector/runtime/interruption-hold')) await delay(120_000);\nassert.equal(formatLabel('alpha'), 'alpha');\nassert.equal(formatLabel('  beta  '), 'beta');\n"
}
